# 13 - 卡点与待确认清单（编码前）

> 生成时间：2026-09-01
> 前置动作：已按你的要求把官方 skills 装到本项目（`.agents/skills/mastra`），
> 所有 Mastra 相关结论优先通过 **skill + `node_modules/@mastra/core/dist/docs/`（embedded docs，440 篇）** 核对，
> 不依赖记忆。凡有疑问处，均以**实测结果**裁决。
>
> 本文只做「卡点识别 + 待决策项」，不含编码。确认后再进入 12 号文档的任务列表。

---

## 零、结论速览

| 类别 | 数量 | 要点 |
|---|---|---|
| **假卡点（已实证排除）** | 3 | 最大一条：skill 说「CommonJS will fail」——**实测不成立**，不需要改模块体系 |
| **硬阻塞（不解决跑不通）** | 2 | K1 Storage、K2 LLM API key |
| **架构债（编码前必须定）** | 1 | K3 闸门输出是 string，条件边无法实现 |
| **待定项** | 3 | K4 编码执行体、K5 gh CLI、K6 GitHub/飞书 adapter 桩 |
| **待你拍板** | 5 | D1~D5，见第三节 |

---

## 一、已实证排除的「假卡点」（不用改，放心跳过）

### 1.1 ❌ "Mastra requires ES2022 modules. CommonJS will fail." —— 不成立

**skill 原文**（`.agents/skills/mastra/SKILL.md:99`）：

> Mastra requires ES2022 modules. CommonJS will fail. See `references/create-mastra.md` ...

这条如果当真，本仓（Midway + `CommonJSFileDetector`）就得推倒重建。所以做了四条独立验证：

| # | 验证方式 | 结果 |
|---|---|---|
| 1 | CJS `require()` 全子路径 | ✅ 全部成功：`@mastra/core`、`/workflows`(71 导出)、`/agent`(45)、`/storage`(401)、`@mastra/koa`、`@mastra/deployer` |
| 2 | `npm run build`（mwtsc） | ✅ 通过，1m38s，零报错 |
| 3 | 生产模式启动 + 打接口 | ✅ 端口 8001 起服；`/api/agents`、`/api/workflows` 均返回 200 且内容正确 |
| 4 | `npx mastra dev`（Studio） | ✅ `Initial bundle complete` → `ready in 430 ms`，Studio 监听 4111 |

**结论**：本仓 Midway CJS + `@mastra/koa` 是官方支持的路径，**不需要迁移到 ES2022 模块体系**。
skill 这条说法过于绝对（大概率只针对 standalone mastra 项目）。

> 处置建议：这是**第三方** skill（`mastra-ai/skills`），直接改文件会在下次 `skills add` 时被覆盖。
> 建议勘误记在本仓（本文档 + `agent.md`），不要改 skill 源文件。

### 1.2 ❌ "Mastra CLI 没装" —— 不存在

`mastra ^1.27.2` 已在 `package.json`，`node_modules/.bin/mastra` 存在，`mastra dev` 实测可用。

### 1.3 ❌ "Mastra 与 Midway 集成不通" —— 不存在

`GET /api/agents` 返回 dev-agent 完整元数据；`GET /api/workflows` 返回 dev-workflow 的 8 个 step 定义。集成已打通。

---

## 二、真卡点

### K1【硬阻塞 · 架构】Storage 未配置 → 人工确认回调（HITL）根本无法工作

**证据 1 — 运行时自己报警**（生产模式启动日志首行）：

```
No `storage` configured on Mastra — falling back to an in-memory store.
In-memory storage is not durable: all data is lost on restart, and it is not safe for production.
```

**证据 2 — A/B 对照实验**（自写探针，不依赖 LLM，结论可复现）：

模拟飞书卡片回调场景：workflow 在 merge 步骤 suspend → 另一个请求只拿 `runId` 恢复。

| 配置 | `createRun({runId})` 后 resume | 结果 |
|---|---|---|
| 无 storage（in-memory） | 同进程、同一 run 对象 | ✅ success |
| 无 storage（in-memory） | 同进程、按 runId 新建 run 对象 | ❌ `This workflow run was not suspended` |
| 无 storage（in-memory） | 新建 Mastra 实例（模拟重启） | ❌ `This workflow run was not suspended` |
| **有 storage（libsql 文件库）** | 新建 Mastra 实例（模拟重启） | ✅ **success，完整恢复** |

**为什么这条最关键**：飞书卡片「点合并」是一个**独立的 HTTP 回调请求**，它手里只有 `runId`，
必须靠 `createRun({ runId })` 重建 run 对象——而上表证明，没有 storage 这条路必失败。

所以 storage **不是"以后再说"的优化项，而是 P1 卡片回调的前置硬依赖**。

**好消息**：解法极轻。已装好 `@mastra/libsql ^1.22.2`（文件库，无需起数据库服务）：

```ts
new Mastra({
  storage: new LibSQLStore({ id: 'mastra-storage', url: 'file:./mastra.db' }),
})
```

⚠️ 注意：本地开发时 `mastra dev` 与应用进程是两个进程，相对路径 `file:./mastra.db` 会被各自 cwd 解析成不同文件，
建议用**绝对路径**，否则 Studio 里看不到应用的 workflow 状态。

---

### K2【硬阻塞 · 运行】LLM API key 全缺 —— 唯一让端到端跑不起来的原因

**实测**（真的把 workflow 跑了一遍，不是推断）：

```
POST /api/workflows/dev-workflow/start-async
{
  "status": "failed",
  "steps": {
    "checkout": { "status": "success", "output": { ..., "branch": "feat/1-dev" } },
    "coding":   { "status": "failed", "error": "Could not find API key
                   process.env.OPENAI_API_KEY for model id openai/gpt-5" }
  }
}
```

- `checkout` **成功执行**（产出 `feat/1-dev`）→ 证明 8 步编排链路是通的
- `coding` 失败于 key 缺失 → **这是当前唯一挡在端到端前面的东西**

**环境核查**：

| 项 | 状态 |
|---|---|
| `.env` 文件 | ❌ 不存在 |
| `OPENAI_API_KEY` | ❌ 未设置 |
| `ANTHROPIC_API_KEY` | ❌ 未设置 |
| GitHub / 飞书 相关环境变量 | ❌ 全为空 |

**影响面**：不阻塞写代码（闸门逻辑可用 mock 写单测）；但**阻塞任何端到端验证**。

---

### K3【架构债 · 编码前必须定】闸门输出是 `string`，条件边无法实现

`src/mastra/workflows/dev-workflow.ts` 现状：

```ts
testResult:   z.string().optional(),
reviewResult: z.string().optional(),
```

设计文档里的「测试失败 → 回 coding 重做」「审核 request changes → 打回」是**条件边（branch/dowhile）**，
它需要一个可判定的布尔/枚举值。字符串没法判定，所以这两条**在架构上目前无法实现**（代码里也确实留了 TODO）。

**解法已确认存在**：embedded docs `docs-agents-structured-output.md` 明确支持
`structuredOutput: { schema: z.object({...}) }`（Zod / Standard JSON Schema 均可）。

**为什么必须在编码前定**：这条一改，`ContextSchema` + 全部 step 的 outputSchema 都要跟着动。
先写完再改 = 全量返工。

---

### K4【待定】编码执行体方案未定

- `@mastra/claude` **未安装**（npm 上最新 0.3.0）
- 官方文档确认 `ClaudeSDKAgent` 支持：
  - `sdkOptions.cwd` —— 这就是「改别的仓库」的落点，正好接你的需求
  - `sdkOptions.allowedTools`、`sdkOptions.mcpServers`、`sdkOptions.model`
  - peer 依赖：`@anthropic-ai/claude-agent-sdk`
- **矛盾点**：`dev-agent` 现在用 `openai/gpt-5`。若引入 Claude 编码 agent，就是**两套 key、两家供应商**。

---

### K5【环境】`gh` CLI 未安装

```
gh --version → command not found
```

- `git 2.39.5` 可用，remote 已配：`git@github.com:wintim1143/pr-agent.git`
- 二选一：`brew install gh`（简单、可交互登录），或用 Octokit 走 REST API（更适合无人值守）

---

### K6【待定】GitHub / 飞书 adapter 全是 TODO 桩

`checkout` / `push-open-pr` / `notify` 三个 step 目前是桩，`prNumber` 硬编码为 `0` 占位。
这部分工作量取决于 D4（目标仓库）与 D5（飞书优先级）。

---

## 三、待你拍板的决策（D1~D5）

| # | 决策点 | 选项 | 我的建议 |
|---|---|---|---|
| **D1** | 模型分工 | ① 统一 Claude（编码+闸门一家，一套 key）<br>② 保持 OpenAI 闸门 + Claude 编码<br>③ 全 OpenAI（不用 `@mastra/claude`） | ① 统一一家。两套 key 只会让调试和成本归因变复杂 |
| **D2** | 编码执行体 | ① `ClaudeSDKAgent`（官方集成，`cwd` 天然支持多仓库）<br>② Mastra agent + 自写文件工具（无新依赖，但编码质量弱）<br>③ 包 CLI 子进程（质量高，工程量大） | ①。官方已收口，且 `cwd` 正好是你要的「定义仓库地址」 |
| **D3** | 权限模式 | ① `allowedTools` 白名单 + `permissionMode: 'dontAsk'`<br>② `acceptEdits`（**注意：Bash 仍需确认，跑测试会挂死**）<br>③ `bypassPermissions`（全放行，**建议列为红线禁用**） | ①。自动化流水线必须无人值守 |
| **D4** | 目标仓库 | ① 先独立 demo 仓库验收<br>② 直接自举（改本仓库） | ①。自举意味着 agent 有能力改 `agent.md`/workflow 本体，需先有受保护路径机制 |
| **D5** | 飞书优先级 | ① 先跑通无 IM 闭环<br>② 先接飞书入口 | ①。主轴没通时，飞书卡片点了也没东西可看 |

**另需你确认的一件事**：飞书 App ID / App Secret **是否已有**？
若没有，飞书这条线会被阻塞，P1 需要先用 HTTP 触发顶上。

---

## 四、建议的解锁顺序（按依赖拓扑，非按阶段）

```
[K3 结构化输出契约]  ←── 编码前必须定，改了要动全部 step
        │
        ├── [K1 Storage]  ←── 3 行代码，但决定 HITL 能不能用
        │
        └── [K2 API key]  ←── 阻塞端到端验证，不阻塞写码
                 │
                 ├── [D1/D2/D3 编码执行体]  ←── 决定 @mastra/claude 装不装
                 │
                 ├── [K5 gh CLI]  ←── 决定 push/OpenPR 走 CLI 还是 Octokit
                 │
                 └── [D5 飞书]  ←── 依赖 K1 先落地
```

**最省事的第一步**：K1 + K3 一起做（都不依赖任何外部凭据，且都是"先定契约再写码"的性质），
做完之后无论 D1~D5 怎么选，返工风险都最小。

---

## 五、随手记录（非阻塞）

- `skills-lock.json` 是 `skills add` 生成的锁文件，目前未纳入 git tracking，建议在提交时一并处理（提交 or 加 .gitignore）。
- 已装依赖变更：`@mastra/libsql ^1.22.2` 新增进 `package.json`。
- 本次验证产生的临时探针文件已清理，未留在仓库中。
