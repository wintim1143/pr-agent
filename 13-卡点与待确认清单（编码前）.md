# 13 - 卡点与待确认清单（编码前）

> 生成时间：2026-09-01
> 前置动作：已按你的要求把官方 skills 装到本项目（`.agents/skills/mastra`），
> 所有 Mastra 相关结论优先通过 **skill + `node_modules/@mastra/core/dist/docs/`（embedded docs，440 篇）** 核对，
> 不依赖记忆。凡有疑问处，均以**实测结果**裁决。
>
> 本文只做「卡点识别 + 待决策项」，不含编码。确认后再进入 12 号文档的任务列表。

---

## 零、结论速览

> **2026-09-03 复核后更新。**核心变化:技术可行性卡点(K1–K8)已全部验证/落地,包括 K8 编码后端(lanfengai)已注入 `coding-agent.ts`、K7-B 权限围栏已落地(采用 `bypassPermissions` + `guard.ts` hook,与用户拍板一致)。
> **K7-A(token 收窄到单仓 + main 分支保护)用户决定不收窄,按当前 fine-grained token 即可**(功能优先,已知风险接受)。
> push 采用 B 路径(用户在 github.com 可达环境手动 push);本环境沙箱代理封 `github.com:443`,故 agent 的 `git push` 在本环境会 502,由用户侧手动推送绕过。
> 飞书 inbound / 真闸门(test·review·commit 真跑) / 端到端首跑 属具体编码内容,推迟到对应阶段(Phase 1/3/4)再实现。

| 类别 | 数量 | 要点 |
|---|---|---|
| **假卡点（已实证排除）** | 3 | 最大一条：skill 说「CommonJS will fail」——**实测不成立**，不需要改模块体系 |
| **🔴 安全缺口（已收敛）** | 1 | **K7**:处置 A 用户决定**不收窄**(按当前 fine-grained token 即可,已知风险接受);处置 B(权限围栏)已落地 —— `bypassPermissions` + `guard.ts` PreToolUse hook(fail-closed),与用户拍板一致 |
| **硬阻塞（不解决跑不通）** | 0 | (无 —— K2 已解决;push 走 B 路径手动推,本环境代理 502 由用户侧绕过) |
| **架构债（编码前必须定）** | 0 | ~~K3~~ 已解决(2026-09-02) |
| **环境（已彻底移除）** | 1 | **K5** `gh` 未装且无 brew → 改走 GitHub REST API;2026-09-03 已清理全部 `gh` 残留(代码 / 验证脚本 / `.env.example` / 注释) |
| **运行降级（已解决）** | 0 | ~~K8~~ 已实测并**已落地**:`coding-agent.ts:122-147` 注入 lanfengai `/v1/messages`(BASE_URL+KEY+MODEL=glm-5.3);之前记录「待注入」为误报 |
| **待你拍板（已收敛）** | 2 | D4 目标仓库(自举 vs demo)、D5 飞书优先级(已倾向先无 IM 闭环);其余 D1/D2/D3 已拍板 |

**当前推进顺序(2026-09-03)**:K1–K8 全部验证/落地;K7-B 权限围栏已落地(`bypassPermissions`);K7-A 用户决定不收窄;K8 已注入。
下一步进入**编码规划(12 号文档 Phase 1–6)**,但飞书 inbound / 真闸门 / 端到端首跑 推迟到对应阶段具体实现;push 走 B 路径(用户手动推送)。

---

## 一、已实证排除的「假卡点」（不用改，放心跳过）

### 1.1 ❌ "Mastra requires ES2022 modules. CommonJS will fail." —— 不成立

**skill 原文**（`.agents/skills/mastra/SKILL.md:99`）：

> Mastra requires ES2022 modules. CommonJS will fail. See `references/create-mastra.md` ...

这条如果当真，本仓（Midway + `CommonJSFileDetector`）就得推倒重建。所以做了四条独立验证：

| # | 验证方式 | 结果 |
|---|---|---|
| 1 | CJS `require()` 全子路径 | ✅ 全部成功：`@mastra/core`、`/workflows`(71 导出)、`/agent`(45)、`/storage`(401)、`@mastra/koa`、`@mastra/deployer` |
| 2 | `npm run build`（mwtsc） | ✅ 通过，1m38s，零报错 |
| 3 | 生产模式启动 + 打接口 | ✅ 端口 8001 起服；`/api/agents`、`/api/workflows` 均返回 200 且内容正确 |
| 4 | `npx mastra dev`（Studio） | ✅ `Initial bundle complete` → `ready in 430 ms`，Studio 监听 4111 |

**结论**：本仓 Midway CJS + `@mastra/koa` 是官方支持的路径，**不需要迁移到 ES2022 模块体系**。
skill 这条说法过于绝对（大概率只针对 standalone mastra 项目）。

> 处置建议：这是**第三方** skill（`mastra-ai/skills`），直接改文件会在下次 `skills add` 时被覆盖。
> 建议勘误记在本仓（本文档 + `agent.md`），不要改 skill 源文件。

### 1.2 ❌ "Mastra CLI 没装" —— 不存在

`mastra ^1.27.2` 已在 `package.json`，`node_modules/.bin/mastra` 存在，`mastra dev` 实测可用。

### 1.3 ❌ "Mastra 与 Midway 集成不通" —— 不存在

`GET /api/agents` 返回 dev-agent 完整元数据；`GET /api/workflows` 返回 dev-workflow 的 8 个 step 定义。集成已打通。

---

## 二、真卡点

### K1【硬阻塞 · 架构】Storage 未配置 → 人工确认回调（HITL）根本无法工作

**证据 1 — 运行时自己报警**（生产模式启动日志首行）：

```
No `storage` configured on Mastra — falling back to an in-memory store.
In-memory storage is not durable: all data is lost on restart, and it is not safe for production.
```

**证据 2 — A/B 对照实验**（自写探针，不依赖 LLM，结论可复现）：

模拟飞书卡片回调场景：workflow 在 merge 步骤 suspend → 另一个请求只拿 `runId` 恢复。

| 配置 | `createRun({runId})` 后 resume | 结果 |
|---|---|---|
| 无 storage（in-memory） | 同进程、同一 run 对象 | ✅ success |
| 无 storage（in-memory） | 同进程、按 runId 新建 run 对象 | ❌ `This workflow run was not suspended` |
| 无 storage（in-memory） | 新建 Mastra 实例（模拟重启） | ❌ `This workflow run was not suspended` |
| **有 storage（libsql 文件库）** | 新建 Mastra 实例（模拟重启） | ✅ **success，完整恢复** |

**为什么这条最关键**：飞书卡片「点合并」是一个**独立的 HTTP 回调请求**，它手里只有 `runId`，
必须靠 `createRun({ runId })` 重建 run 对象——而上表证明，没有 storage 这条路必失败。

所以 storage **不是"以后再说"的优化项，而是 P1 卡片回调的前置硬依赖**。

**好消息**：解法极轻。已装好 `@mastra/libsql ^1.22.2`（文件库，无需起数据库服务）：

```ts
new Mastra({
  storage: new LibSQLStore({ id: 'mastra-storage', url: 'file:./mastra.db' }),
})
```

⚠️ 注意：本地开发时 `mastra dev` 与应用进程是两个进程，相对路径 `file:./mastra.db` 会被各自 cwd 解析成不同文件，
建议用**绝对路径**，否则 Studio 里看不到应用的 workflow 状态。

---

### K2【硬阻塞 · 运行】LLM API key 全缺 —— ✅ 已解决(2026-09-02)

> 原实测错误:`Could not find API key process.env.OPENAI_API_KEY for model id openai/gpt-5`。
> 现在模型名 / key / URL **全部改为环境变量驱动**,且改用 `OpenAICompatibleConfig` 适配中转站。

**解法**（新增 `src/mastra/config.ts` + 改 `src/mastra/agents/dev-agent.ts`）：

- 用 Mastra 内建 `OpenAICompatibleConfig` 做模型配置,运行时被解析为
  `createOpenAICompatible({ name, apiKey, baseURL: url }).chatModel(modelId)`,
  即标准 **OpenAI Chat Completions**(`/chat/completions`)。
  原因:中转站(one-api / new-api / 各类 relay)几乎只实现 Chat Completions,Mastra 默认的
  `openai/<model>` 走 Responses API,多数中转站不兼容。
- 四项环境变量(模板见仓库根 `.env.example`):

  | 变量 | 含义 | 必填 |
  |---|---|---|
  | `LLM_PROVIDER` | 自定义 provider 名(仅标识,默认 `relay`) | 否 |
  | `LLM_MODEL` | 中转站模型名,如 `gpt-5` / `deepseek-chat` | ✅ |
  | `LLM_BASE_URL` | 中转站 base URL,须到 `/v1` 这一级 | ✅ |
  | `LLM_API_KEY` | 中转站密钥 | ✅ |

- `dev-agent.ts` 的 `model` 由 `'openai/gpt-5'` 改为 `llmModelConfig`,不再硬编码。
- 模块加载**不抛错**(否则会拖垮 `npm test` 与 `GET /api/agents` 仅列元数据);缺失配置只在
  `agent.generate()` 时由 Mastra 暴露清晰错误。可调用 `missingLlmConfig()` 提前校验。

**验证状态**：

- `npm run build` ✅、`npm test` ✅(4/4,config 模块加载无副作用)。
- `scripts/verify-llm.js` 结构校验 ✅:`resolveModelConfig(llmModelConfig)` 解析为
  `ModelRouterLanguageModel`(含 `doGenerate`/`doStream`),证明中转站 wiring 正确。
- **真实 ping 待你填 `.env` 后跑**:`cp .env.example .env` 填好三项,再 `node scripts/verify-llm.js`,
  会用 dev-agent 向中转站发一条最小请求验证 key/URL/model 真能用。

**环境核查**(底稿,供对照):原 `.env` / `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` 均缺失;
现改为 `LLM_*` 一组,且 `.env` 已被 `.gitignore` 忽略。

**影响面**:原「阻塞任何端到端验证」已解除 —— 配齐 `LLM_*` 三项即可跑通。

---

### K3【架构债 · 编码前必须定】闸门输出是 `string`，条件边无法实现 —— ✅ 已解决(2026-09-02)

`src/mastra/workflows/dev-workflow.ts` 现状（已改，commit `39a4c5b`）：

```ts
TestGateSchema   = { passed: boolean, report: string }
ReviewGateSchema = { decision: 'approve' | 'request-changes', comments: string[] }
CommitGateSchema = { message: string, lintPassed: boolean }

// ContextSchema 里:
testResult:   TestGateSchema.optional(),
reviewResult: ReviewGateSchema.optional(),
commitResult: CommitGateSchema.optional(),   // 原 commitMessage(string) 更名
```

设计文档里的「测试失败 → 回 coding 重做」「审核 request changes → 打回」是**条件边（branch/dowhile）**，
它需要一个可判定的布尔/枚举值。现在有了结构化 schema，条件边可以按 `testResult.passed` /
`reviewResult.decision` 判定，不再需要解析字符串。

**落地方式**（已实测验证）：
- `runGate<S>(mastra, instruction, schema)` 泛型：传 schema 时走 `agent.generate` 的
  `structuredOutput: { schema, errorStrategy: 'strict' }`，从 `res.object` 读结构化结果
- 纯文本 step（coding / merge）改用 `runPlain`
- **踩坑**：`runGate` 原用"可选 schema + 展开对象"写法，TS 无法匹配 `generate` 重载
  （无 structuredOutput 时重载要求 `structuredOutput?: never`）；拆成两个函数解决
- **API 事实**（`agent.d.ts` 权威）：`generate(msg, {structuredOutput})` 返回 `FullOutput`，
  结构化结果在 `.object`；重载区分"有/无 structuredOutput"
- **验证**：build 通过；全量测试 4/4；探针实测 workflow 能构造（schema 被 Mastra 接受）、
  checkout step success、链路跑到 coding（缺 key 失败为预期）

**遗留**：P3-5 条件边（test 失败 / review request changes → 回 coding 重做 + 重试上限）
**未实现**，依赖测试/审核闸门真正运行（Phase 2/3）后再接。`dev-workflow.ts` 里已留 TODO 标注。


---

### K4【已解决·已装】编码执行体 = `@mastra/claude` 的 `ClaudeSDKAgent`（2026-09-02 决策）

- **决策（用户拍板）**：Phase 0 `coding` 步若用 Mastra 原生 agent 做"真正写文件"，需自造 read/write/edit/bash/glob/grep 全套 `createTool` 工具链（工作量大、权限/沙箱/错误恢复边界易漏）；
  而 `@mastra/claude` 的 `ClaudeSDKAgent` 封装 Claude Code CLI，**原生全套文件/命令工具 + `sdkOptions.cwd`(目标仓库) + `allowedTools` + `permissionMode`(无人值守) + `mcpServers`**，对接「改别的仓库 + 无人值守流水线」明显更简单 → **安装**。
- **已装**：`npm install @mastra/claude@0.3.0 @anthropic-ai/claude-agent-sdk`（added 48 packages）。CJS 导出 `ClaudeSDKAgent` 验证可加载；`@mastra/core@1.63.2` 满足 peer(`>=1.34.0`)。
- `ClaudeSDKOptions = ClaudeQueryOptions`（来自 `@anthropic-ai/claude-agent-sdk`），`sdkOptions` 透传到每次 `query()`，支持 `cwd`/`allowedTools`/`permissionMode`/`mcpServers`/`resume`/`continue`（含会话续跑——天然支持 P1 长任务断点恢复）。
- **代价（已认知）**：引入第二套 key——编码体用 **Anthropic/Claude**（需 `ANTHROPIC_API_KEY` 或 Claude Code 登录态），闸门仍用中转站 `OpenAICompatibleConfig`(glm-5.2 等)。即 D1 现状 = **两套供应商**：编码 Claude + 闸门中转站。这是用户知情接受的取舍。
- **已接（代码层，2026-09-02）**：`coding` 步已从「dev-agent 纯文本」改为 `ClaudeSDKAgent` 真正写文件。新建 `src/mastra/agents/coding-agent.ts`（`getCodingAgent(cwd)` 内执行期懒加载 `@mastra/claude`，顶层仅 `import type` 不触发运行时加载）+ `dev-workflow.ts` coding 步 `await import('../agents/coding-agent.js')` 动态加载。隔离原因：`@mastra/claude` 的 ESM 依赖 `@anthropic-ai/claude-agent-sdk/sdk.mjs` 在 jest/部分运行时解析失败，曾导致 Midway app 启动(createApp)失败、`npm test` 全红；隔离到执行期后 `npm test` 恢复 4/4。`missingClaudeKey()` 检测 `ANTHROPIC_API_KEY`/`CLAUDE_API_KEY` 缺失时降级跳过、不阻断流程。
- **待真跑**：需配 `ANTHROPIC_API_KEY`（或 Claude Code 登录态）；`permissionMode: 'bypassPermissions'` 是权限红线，生产应改白名单 + `default`/`acceptEdits`。真实 PR 内容将由 Claude 写入的文件决定（不再是空 PR）。

---

### K5【环境】`gh` CLI 未安装 → ✅ 绕开完成(2026-09-02 决策) + ✅ 残留清理(2026-09-03)

```
# macOS 实测(2026-09-02)
gh   --version → command not found
brew --version → command not found   ← 装 gh 的前提本身也不存在
```

- 原计划「装 gh」在 macOS 上成本高于预期(需先装 Homebrew,再装 gh,再 `gh auth login` 交互登录)。
- **决策:不引入 `gh` 依赖,全部改走 GitHub REST API**。理由:
  1. `gh` 在本仓库只承担两件事:`gh auth status`(只读探测)与 `gh pr create`(开 PR);
  2. 两者都有等价 REST 端点:`GET /user` + `GET /repos/{o}/{r}` 替代前者,
     `POST /repos/{o}/{r}/pulls` 替代后者,`PUT /repos/{o}/{r}/pulls/{n}/merge` 替代 `gh pr merge --squash`;
  3. `GITHUB_TOKEN` 已在手且**实测有效**(见 K6),不需要 gh 的 OAuth 登录态;
  4. 少一个外部二进制 → Windows / macOS / Linux 行为一致,更契合无人值守目标。
- **待办已全部落地(2026-09-03 实测)**:
  - ✅ `github.ts` 的 `push-open-pr`:`gh pr create` → `POST /repos/{o}/{r}/pulls`
    (`openPrForBranch()` 先查该 head 已 open 的 PR 复用,避免 422 重复建);
  - ✅ `merge` 步:`gh pr merge --squash` → `PUT /repos/{o}/{r}/pulls/{n}/merge`(`merge_method: 'squash'`);
  - ✅ `scripts/verify-github.js`:`gh auth status` → REST 只读探测
    (`GET /user` + `GET /repos/{o}/{r}` + `GET /branches/{base}`,全部不创建资源,exit 0);
  - ✅ `.env.example`:删掉假的「方案 A(`gh auth login` 免建 token)」与 `GH_PATH`
    —— `getGithubConfig()` 从未支持过 gh OAuth 路径,按那说明配置会失败;
  - ✅ `dev-workflow.ts` 两处注释:`gh pr create` → REST `POST /pulls`。
- **⚠️ 踩坑记录(本次实测)**:改成纯 REST 时把 `GithubConfig` 的 `useGhAuth` / `ghPath` 字段删了,
  但 `verify-github.js` 仍引用它们 → 脚本**无论装没装 gh 都必失败**,报
  `The "file" argument must be of type string. Received undefined`,而它打印的三条「可能原因」
  全都不包含真实原因,排查时会被带偏。
  **教训:删接口字段时,必须同时搜调用方(含 `scripts/` 与文档),不能只过编译。**

---

### K6【部分已解】GitHub / 飞书 adapter

- **飞书 notify adapter 已实现**（2026-09-02）：`src/mastra/adapters/feishu.ts` 支持两种模式——群机器人 Webhook(`FEISHU_WEBHOOK_URL`,可选 `FEISHU_WEBHOOK_SECRET` 签名)与自建应用(`FEISHU_APP_ID`+`FEISHU_APP_SECRET`+`FEISHU_RECEIVE_ID`+`FEISHU_RECEIVE_ID_TYPE`)。workflow 的 `notify` 步已接入,推「开发完成」interactive 卡片(合并/拒绝按钮,`callback_id` 内嵌 issue 号)。未配置/推送失败仅告警不阻断流程。`scripts/verify-feishu.js` 可发真实测试卡片验证。
  - **按钮回调(点合并→resume)属 P1 inbound 工作**:需要飞书事件订阅 / 回调服务器,本次未做,卡片按钮点了暂无后端响应。
  - **自建应用真实推送已验证通过**(2026-09-02):`node scripts/feishu-list-chats.js` 用 App ID/Secret 成功拿 token 并列出群 `mcp群组`;`node scripts/verify-feishu.js` 真实推送卡片到该群成功(mode=app)。飞书通知链路完整打通。Webhook 模式代码完成但未经真实推送验证。
- **GitHub adapter 已实现**(2026-09-02 后续):`src/mastra/adapters/github.ts` 落地 `checkout` + `commit` + `push-open-pr` 三步。
  - `checkout` 用本地 `git` 建并切到 `feat/<n>-<slug>`(从 `GITHUB_BASE_BRANCH` 默认 main 分出);
  - `commit` 步现在**真正 `git add -A` + `git commit`**(之前只生成 message 文本不落盘);
  - `push-open-pr` 用 `git push`(token 内嵌 HTTPS URL,不依赖 SSH/credential helper)+ **REST `POST /repos/{o}/{r}/pulls`** 开 PR,返回真实 `prNumber`/`prUrl`(2026-09-03 已去 gh 化)。
  - 配置最少只需 `GITHUB_TOKEN`(**fine-grained,Contents + Pull requests 写权限**);`GITHUB_OWNER`/`GITHUB_REPO` 留空自动从 `git remote get-url origin` 解析(本机解析结果:`wintim1143/pr-agent`,remote 为 SSH 形态 `git@github.com:wintim1143/pr-agent.git`)。`GH_PATH` 已随 K5 清理删除。
  - **2026-09-02 实测(REST 直连,绕开未安装的 `gh`)**:
    - `GET /user` → 200,login = `wintim1143`;
    - `GET /repos/wintim1143/pr-agent` → 200,`permissions = {admin, maintain, push, triage, pull}` 全 true;
    - `default_branch = main`,base ref `refs/heads/main` 可读;
    - 分支 / PR / issue / contents / workflows 五类只读端点全部 200 → **开 PR 链路具备条件**;
    - rate limit `5000/5000`(未被消耗)。
  - 未配置 GitHub → 两步跳过、`prNumber` 保持 0、不阻断后续 notify/merge。
  - ~~`scripts/verify-github.js` 仍调 `gh auth status`~~ → **已随 K5 修掉(2026-09-03)**,改为 REST 只读探测。
  - **仍缺端到端验证**:起服务触发 workflow 真实开一次 PR(当前 `coding` 步因 K8 会降级空转,PR 可能为空)。
  - **⚠️ 凭据面两个问题已单列为 K7,优先级高于本条。**

---

### K7【安全 · 编码前必须收敛】凭据权限过宽 + `main` 分支零保护(2026-09-02 实测)

四项叠加后,编码 agent **已具备直接改写 `main` 的能力**,且仓库层面无任何拦截:

| 项 | 实测结果 | 风险 |
|---|---|---|
| `GITHUB_TOKEN` scope | classic PAT(40 位)。`x-oauth-scopes = admin:org_hook, admin:repo_hook, audit_log, codespace, project, repo, user, workflow, write:discussion, write:network_configurations, write:packages` | 远超流水线所需的 `repo`。泄露后可跨仓库操作、改 org 级 webhook |
| `main` 分支保护 | `GET /repos/wintim1143/pr-agent/branches/main/protection` → **404,无任何保护规则** | 允许直接 push、允许 force push、无 required review、无 status check |
| 编码 agent 权限 | `coding-agent.ts:56` 写死 `permissionMode: 'bypassPermissions'`(即 D3 选项③,文档自列**红线禁用**) | agent 写文件 / 跑命令均无需确认,`allowedTools` 白名单不生效 |
| 受保护路径 | `agent.md` / `.github/` / `src/mastra/{workflows,agents}/` **零拦截** | agent 可修改 workflow 本体与自身行为约束 |

> **收敛结论(2026-09-03)**:用户已拍板 —— 普通 `push main` 允许(本仓库直接开发),`force push` / `rebase main` 仍由 `guard.ts` 拦截;main 分支保护暂不设置(功能优先,已知风险接受)。
> 受保护路径(`agent.md`/`.github/**`/`.env*`/`src/mastra/{workflows,agents}/**`)由 `guard.ts` PreToolUse hook 硬拦(fail-closed),agent 无法自改行为约束。
> 凭据面 K7-A 不收窄(token 仍 All repositories fine-grained),爆炸半径已比原 classic PAT 小(无 org hook / audit_log 等),用户接受该风险。

**二次实测（用户已换 fine-grained PAT 之后,2026-09-02 22:10）**

| 操作 | 实测 | 判定 |
|---|---|---|
| 读仓库 / 列 PR / 列分支 / 读 contents | 全部 200 | ✅ 只读正常 |
| **开 PR** `POST /repos/{o}/{r}/pulls`(带 title/head/base) | **403** | ❌ **流水线核心能力缺失** |
| **写文件** `PUT /repos/{o}/{r}/contents`(带 message/content) | **403** | ❌ 缺失 |
| **建分支** `POST /repos/{o}/{r}/git/refs`(带 ref/sha) | **403** | ❌ 缺失 |
| 改仓库设置 `PATCH /repos` | 403 | ✅ 本就不该有 |
| org 级(读 org / org hooks) | 404 | ✅ 已隔离,优于原 classic PAT |

→ **结论:新 token 实际处于只读级别,开不了 PR。需补 `Contents: Read and write` + `Pull requests: Read and write`。**
另:Repository access 仍是 **All repositories**(`/user/repos` 返回 31 个仓库),建议改为 Only select repositories → `pr-agent`。

**三次实测(用户换 fine-grained token 之后,2026-09-03 11:58)**

| 项 | 实测 | 判定 |
|---|---|---|
| token 类型 | 前缀 `github_pat_`(93 位);`X-OAuth-Scopes` 响应头缺失 | ✅ 已换成 fine-grained(fine-grained 不走 OAuth scope,该头缺失是其特征) |
| Contents 写权限 | `git push --dry-run` 建新分支 → `* [new branch]` + exit 0 | ✅ 已具备 |
| Repository access | `GET /user/repos` 返回 **31 个仓库** | ❌ 仍 All repositories,未收窄 |
| `main` 分支保护 | `GET /repos/{o}/{r}/branches/main` → `protected: false` | ❌ 未设置 |
| Pull requests 写权限 | 未验证 | ❓ 无无损探测手段 |

> **⚠️ 验证分支保护不能看 `protection` 端点。** fine-grained token 无 Administration 权限时,
> `GET /branches/main/protection` 一律返回 **403 "Resource not accessible by personal access token"** ——
> 这个 403 既不代表有保护、也不代表没保护,只是读不到。
> **正确无损办法**:`GET /repos/{o}/{r}/branches/{b}` 的 **`protected` 字段**(只需 Contents: Read,
> 且同时覆盖 classic protection 与 rulesets)。
>
> 另:`git push --dry-run` 可以用来无损探测 contents 写权限,但**验证不了分支保护** ——
> 本地与远程一致时 git 输出 "Everything up-to-date" 并在发 pack 前短路,服务端没机会拒绝(假阴性)。

> **用户决策(2026-09-03 14:06):分支保护暂不处理,功能优先。** 上述 K7-A 两项挂起,不占关键路径。

> **⚠️ 关键认知修正:`GET /repos/{o}/{r}` 返回的 `permissions` 字段对 fine-grained PAT 完全不可信。**
> 该字段返回 `{admin,maintain,push,triage,pull}` 全 true,但同一 token 的 `PATCH /repos` 却是 403。
> 它反映的是**你作为 owner 的固有权限**,不是 token 的授权范围。
> 判断 fine-grained token 到底能干什么,只能靠**实际端点探测**,不能看这个字段。

> **⚠️ 探测写权限的方法警告(本次踩坑)**:GitHub **没有无损探测写权限的办法**。
> - 空 body → 422 是「请求体校验失败」,发生在**鉴权之前**,会被误判成「有写权限」;
> - 格式合法但业务无效的 body → **可能真的创建资源**(本次误建了 issue #1);
> - 对不存在的资源操作 → 404 与 403 无法可靠区分(merge 探测返回 404 即假阳性)。
>
> 正确做法:**让用户直接截图 token 的 Permissions 勾选页**,或在一个一次性 demo 仓库上做真实操作验证。
> 不要在真实仓库上探测。

**处置 A（非编码 · 需你在 GitHub 网页操作,约 5 分钟）**

1. **修正已建的 fine-grained PAT** —— token 类型已经换对了,但**授权范围不对**。
   `Settings → Developer settings → Personal access tokens → Fine-grained tokens → 点进该 token → Edit`
   - **Repository access**:当前是 `All repositories`(实测可见 31 个仓库)→ 改为 **Only select repositories → 只选 `pr-agent`**
   - **Repository permissions**:补 **`Contents: Read and write`** 与 **`Pull requests: Read and write`**;
     `Metadata: Read-only` 默认已有。若勾了 `Administration` / `Actions` / `Secrets`,去掉
   - 保存后旧 token 立即失效,把新值重新填进 `.env` 的 `GITHUB_TOKEN`
   - **验收**:开 PR / 写文件 / 建分支 三项探测不再是 403
2. **给 `main` 加保护** —— **仍未做**(2026-09-02 22:10 复查,`protection` 接口依旧 404)。
   `Settings → Branches → Add branch ruleset`(或 classic protection),至少勾:
   - `Restrict deletions`
   - `Require a pull request before merging`(Required approvals ≥ 1)
   - `Block force pushes`
   - 推荐再加 `Require status checks to pass` → 选 CI 的 build / test
   - 勾上后 **feature 分支不受影响**,开 PR 与 merge 仍走 token,不影响流水线

**处置 B（已落地 2026-09-03 · 采用 `bypassPermissions`）**

> 用户拍板:编码 Agent 统一用 `permissionMode: 'bypassPermissions'` + `allowDangerouslySkipPermissions: true`(无人值守零弹窗);
> 真正的硬拦截靠 PreToolUse hook → `guard.ts`(fail-closed,与 permissionMode 无关)。`allowedTools`/`disallowedTools` 仅作防御纵深。

3. ✅ `coding-agent.ts:132` `permissionMode: 'bypassPermissions'` + `allowDangerouslySkipPermissions` 已落地;`guard.ts` 作硬拦截(57+3 用例绿)。
4. ✅ 受保护路径拦截已实现:`guard.ts` 禁写 `agent.md`/`.github/**`/`.env*`/`src/mastra/{workflows,agents}/**`(fail-closed)。
5. ✅ `github.ts`:含斜杠分支的 ref 假成功已由 `createBranchVerified`/`ensureRefFlushed` 用 `mkdirSync+writeFileSync` 兜底;`git push` 前断言非 base 分支由 `guard.ts` 危险命令兜底(含 force push)。

---

### K8【运行】编码后端真相:本地 Claude CLI → cc-switch → opencode 余额墙 → 需切 lanfengai(2026-09-02 实测决定性)

> **一句话结论:Mastra 编码 agent 走的是「本地 Claude Code CLI」这个进程模型,这台机器
> 的真实编码能力不取决于有没有 `ANTHROPIC_API_KEY`,而取决于**本地 claude 进程背后连的
> 中转端点能不能调通**。当前它被 `~/.claude/settings.json` 指向 cc-switch→opencode(余额不足,
> 必失败);**实测 lanfengai 支持 Anthropic Messages 协议,可作编码后端**。

#### 完整证据链(全一手实测,2026-09-02 晚)

1. **SDK 拉的是原生 claude 二进制,非直连 Anthropic HTTP**:
   `@anthropic-ai/claude-agent-sdk` v0.3.258 通过 optionalDependencies 装平台原生二进制
   `@anthropic-ai/claude-agent-sdk-darwin-arm64/claude`(199MB, v2.1.258)。
   SDK 的 `getDefaultExecutable()` 返回 node/bun,但实际 fork/驱动的是这个**原生 claude 二进制** →
   **本地进程模型**。`@mastra/claude` 的 `ClaudeSDKAgent` 底层即此 SDK,故 env 行为以该 SDK 源码为准。
2. **本机 `claude` 不在 PATH,但 `~/.claude/` 很全**,`settings.json` 的 env 段为:
   - `ANTHROPIC_BASE_URL: http://127.0.0.1:15721`(本地代理)
   - `ANTHROPIC_AUTH_TOKEN: "PROXY_MANAGED"`(代理伪 token,非真 key)
   - `ANTHROPIC_DEFAULT_*_MODEL_NAME: deepseek-v4-flash`(模型被映射)
3. **端口 15721 由 cc-switch(PID 3081)监听**,curl 其 `/v1/messages` → 转发到 **opencode.ai**,
   返回 `CreditsError: Insufficient balance` → **coding 走 cc-switch 必失败**(opencode 账户余额不足)。
4. **lanfengai 中转支持 Anthropic Messages 协议(决定性)**:`POST https://lanfengai.cn/v1/messages`
   用项目已有 `LLM_API_KEY` + 模型 **`glm-5.3`** → **200 真实返回** `{"type":"message",...,"text":"ok"}`。
   - 模型名必须用 lanfengai 自己的(如 `glm-5.3`);用 `claude-sonnet-4-6` 报 `Public model was not found`。
   - 踩坑:读 .env 须 `dotenv.config({quiet:true})`,否则 `◇ injected env` 日志污染取到的变量值(curl 因此 HTTP=000)。

#### 推论与落地(编码 agent 侧,**已落地 2026-09-03**)

- ✅ 已注入:`coding-agent.ts:122-147` 显式注入 `ANTHROPIC_BASE_URL=LLM_BASE_URL`(lanfengai.cn/v1,Anthropic Messages 协议)+ `ANTHROPIC_API_KEY=LLM_API_KEY` + `ANTHROPIC_MODEL=LLM_MODEL||'glm-5.3'`,本地 claude 不再走 cc-switch→opencode 余额墙。
- **不需要配官方 `ANTHROPIC_API_KEY`**(之前 `missingClaudeKey()` 只看该变量是过时逻辑,已随注入修正:缺 `LLM_API_KEY` 才告警)。
- 已证伪的旧猜测:lanfengai 走 OpenAI Chat 协议不能复用 —— **实测其同时暴露 Anthropic Messages 端点**(`/v1/messages` 200),直接复用 `LLM_API_KEY`,无需另配。
- `.env` 现状:`ANTHROPIC_*` 四项全空无碍(走注入);`LLM_*` 三项需填(`.env.example` 模板)。

---

## 三、待你拍板的决策（D1~D5）

| # | 决策点 | 选项 | 状态 / 结论 |
|---|---|---|---|
| **D1** | 模型分工 | ① 统一 Claude（编码+闸门一家）<br>② 保持 OpenAI 闸门 + Claude 编码<br>③ 全 OpenAI | ✅ **已拍板:②**——闸门用中转站(`LLM_*`/glm 系),编码用 ClaudeSDKAgent 走 lanfengai(已注入)。两套 key 接受 |
| **D2** | 编码执行体 | ① `ClaudeSDKAgent`<br>② Mastra agent + 自写文件工具<br>③ 包 CLI 子进程 | ✅ **已拍板:①**——`@mastra/claude` 已装并注册 `coding-agent` |
| **D3** | 权限模式 | ① `allowedTools`+`dontAsk`<br>② `acceptEdits`<br>③ `bypassPermissions` | ✅ **已拍板:③**——`bypassPermissions`+`allowDangerouslySkipPermissions`;硬拦截靠 `guard.ts` PreToolUse hook(fail-closed)。`12 号 §1.2` 以本条为准 |
| **D4** | 目标仓库 | ① 先独立 demo 仓库验收<br>② 直接自举（改本仓库） | 🟠 待拍板:倾向 ② 自举(本仓库已允许直接改 main),但需先确认自举时受保护路径机制是否足够 |
| **D5** | 飞书优先级 | ① 先跑通无 IM 闭环<br>② 先接飞书入口 | ✅ **已拍板:①**——飞书 inbound 推迟到 Phase 1 具体实现,先用 HTTP 触发跑通主轴 |

**另需你确认的一件事**：飞书 App ID / App Secret **是否已有**？
若没有，飞书这条线会被阻塞，P1 需要先用 HTTP 触发顶上。

---

## 四、解锁顺序与执行排期（2026-09-02 修订）

### 4.1 依赖拓扑（现状）

```
[K3 结构化输出] ✅ 已解决(2026-09-02, commit 39a4c5b)
[K1 Storage]    ✅ 已解决(commit 8e50a93)
[K2 LLM key]    ✅ 已解决(2026-09-02) —— 中转站 relay/glm-5.3 真实调用返回 pong
[K4 编码执行体] ✅ 已解决(@mastra/claude 0.3.0 已装)
[K5 gh CLI]     ✅ 决定绕开 —— 全部改走 GitHub REST API
[K6 GitHub 鉴权] ✅ 实测通过(permissions 全 true,五类只读端点全 200)
        │
        ├── [K7-A 凭据收敛] 🟠 你做: 收窄 fine-grained PAT 到单仓(当前仍 All 31 仓)+ main 分支保护
        ├── [K7-B 权限围栏] ✅ 已落地(feat/4-permission-guard):方案 A = bypassPermissions
        │        │        + allowDangerouslySkipPermissions + PreToolUse hook→guard.ts 硬拦
        │        │        (受保护路径 + 危险命令 + fail-closed);57 guard 用例 + 3 hook 用例全绿
        │        │
        │        └── [K8 编码后端] 🟠 我已实测、你来拍板注入:lanfengai `/v1/messages`(Messages 协议)
        │                 │       实测 200。需 coding-agent 显式注入 BASE_URL+KEY+MODEL=glm-5.3,
        │                 │       否则本地 claude 走 cc-switch→opencode(余额不足,必失败)
        │                 └── [端到端首跑] → 真实 PR 落地
        │
        ├── [C4 RepoTarget] ✅ 经实测非硬编码:`repoRoot()`/`getRepoRoot()` 均用 `git rev-parse --show-toplevel`,不构成阻塞(13 号文档原记「硬编码」为漂移,已纠正)
        ├── [C5 真闸门]     🟠 test 真跑 npm test / review 喂真实 diff / commit 跑 commitlint
        └── [C6 merge]      🟠 接 PUT /repos/{o}/{r}/pulls/{n}/merge
```

### 4.2 执行排期（非编码先行）

| 序 | 事项 | 类型 | 谁 | 依赖 | 验收标准 / 状态 |
|---|---|---|---|---|---|
| **1** | fine-grained PAT | 非编码 | 你 | — | ✅ **已完成**:token 类型已换 `github_pat_`,Contents+Pull requests 写权限具备;**Repository access 仍 All repositories 31 仓 —— 用户决定不收窄,已知风险接受** |
| **2** | `main` 分支保护 | 非编码 | 你 | — | ⏸ **用户决定挂起(功能优先)**;普通 push main 允许,force push/rebase 由 guard.ts 拦截 |
| **3** | 编码后端注入 lanfengai | 编码 | 我 | — | ✅ **已落地(2026-09-03)**:`coding-agent.ts:122-147` 注入 BASE_URL+KEY+MODEL=glm-5.3 |
| **4** | 权限围栏(`bypassPermissions` + guard.ts) | 编码 | 我 | 1,2 | ✅ **已落地**:agent 写 `agent.md`/`.github/**` 被拒(57+3 用例绿) |
| **5** | push 护栏(禁 force / 非 base 分支) | 编码 | 我 | 2 | ✅ **已落地**:`guard.ts` 危险命令含 force push;`github.ts` push 前断言由 hook 兜底 |
| **6** | `verify-github.js` REST 探测 | 编码 | 我 | — | ✅ **已完成(2026-09-03)**:exit 0;`gh` 残留清掉 |
| **7** | GitHub adapter 去 gh 化 | 编码 | 我 | 1 | ✅ **已完成**:`POST /pulls` + `PUT /pulls/{n}/merge`(代码) |
| **8** | RepoTarget `repoPath` 配置项 | 编码 | 我 | — | ✅ **经实测非必须**:`repoRoot()`/`getRepoRoot()` 已用 `git rev-parse --show-toplevel`,非硬编码;Phase 6 多仓库时再补配置项 |
| **9** | 端到端首跑(真实 PR) | 验证 | 你+我 | 3,4,7 | 🟠 **推迟**:push 走 B 路径(用户手动推);本环境代理 502 下由用户侧完成。属具体编码验证,到 Phase 4 再做 |
| **10** | 真闸门(test/review/commit 真跑) | 编码 | 我 | 9 | 🟠 **推迟到 Phase 3**:目前为 TODO 空壳,到 P3-2/3-3/3-4 实现 |
| **11** | P1 飞书 inbound | 编码 | 我 | 9 | 🟠 **推迟到 Phase 1**:卡片按钮 resume 属具体编码,先用 HTTP 触发跑通主轴 |

**关键路径**:`1 → 2 → 4 → 7 → 9`。第 3 项可与 1、2 并行准备,不占关键路径。
**原则**:任何一项编码工作开始前,K7 的 A + B 必须都落地——先关笼子,再放 agent。

---

## 五、随手记录（非阻塞）

### 5.1 文档勘误

- ~~`skills-lock.json` 未纳入 git tracking~~ —— **此条已过时**。
  2026-09-02 复核:`git ls-files` 确认该文件**已被跟踪**,且未被 `.gitignore` 排除。
  结论:保持跟踪即可(它锁定了 `mastra` skill 的 source 与 `computedHash`,属于应当共享的信息),无需处理。
- 已装依赖变更:`@mastra/libsql ^1.22.2` 新增进 `package.json`。

### 5.2 环境坑（踩过一次，别再踩）

1. **`dist/` 陈旧会让三个 verify 脚本全部误报。**
   `scripts/verify-*.js` 都是 `require('../dist/mastra/...')`,而 `dist/` 在 `.gitignore` 里、
   且不会随 `src/` 改动自动更新。**任何一次 `src/` 改动后、跑 verify 脚本前,必须先 `npm run build`**,
   否则看到的是 `Cannot find module '../dist/mastra/config.js'`,极容易被误判成「配置有问题」。
2. **`node_modules` 与 `package-lock.json` 会不同步。**
   2026-09-02 实测:lock 里有 `@mastra/claude@0.3.0 (resolved)`,但 `node_modules/@mastra/claude`
   目录根本不存在 → `npm run build` 直接 `TS2307` 失败。补装 48 个包后恢复。
   **排查 build 失败时,先确认包目录是否真的存在,不要只信 lock。**
3. **`npm install` 会改写 lock 里的 registry。**
   本机 lock 原本记录 `registry.npmmirror.com`,补装后被改写为 `registry.npmjs.org`(单行 diff)。
   这属于**个人环境偏好,不应进仓库**,已手工回退。以后补装完记得 `git diff package-lock.json` 扫一眼。
4. **临时脚本要带 `NODE_PATH`。**
   在 `/tmp` 写探针脚本时 `require` 项目依赖会失败,需:
   `NODE_PATH=<repo>/node_modules node /tmp/xxx.js`。
5. **沙箱代理只放行 `api.github.com`,封 `github.com:443`。**
   本环境 `https_proxy=127.0.0.1:10917` 的 allowlist 仅放行 `api.github.com`(REST API 通,开 PR/merge 可用),
   封掉 `github.com:443` 的 git/SSH 传输 → `git push`/`ls-remote`/`fetch` 在本环境**永远 502**。
   影响:agent 的 `git push`(`github.ts:380`)在本环境推不出去;但开 PR/merge 走 REST 不受影响。
   **处置(push 路径 B)**:用户在 github.com 可达的机器上手动 `git push` agent 生成的分支并跑端到端,本环境只负责编码与 REST 调用。
   另:含斜杠分支名(`feat/123-xxx`)在本环境 PortableGit 下 `git branch`/`update-ref` 会假成功(退出 0 却不建 ref 目录),`github.ts` 的 `createBranchVerified`/`ensureRefFlushed` 已用 `mkdirSync+writeFileSync` 兜底(详见 `github.ts:171` 注释)。

### 5.3 本次已清理

- `.claude/`(ClaudeSDKAgent 运行时生成的本地目录)已加入 `.gitignore`。
- `package-lock.json` 的 registry 改动已回退。
- 临时探针脚本均写在 `/tmp`,未落在仓库中。
