# M2 · 本地写入闭环（Local Write Loop）

状态：**进行中**（M2-1/M2-2 已完成，M2-3 编排改造未开始）
创建日期：2026-09-07

> **填写分层（先定义、后完善）**
> - 🟦 **定义期必填**：头部角色责任块 + 第 1–4 节。
> - 🟨 **实施期渐进补全**：第 5–11 节，推进到对应阶段再填。
> - ⚠️ **源码级证据只进第 11 节「实施日志·已核实事实」**（行号、实测命令、接口路径）。
> - 📊 数据流向图见 [`M2-本地写入闭环-flow.md`](./M2-本地写入闭环-flow.md)。

---

## 0. 角色与责任 🟦

| 角色 | 是谁 | 在本里程碑做什么 / 何时介入 |
|---|---|---|
| 触发方 | **开发者手动触发**（脚本 / curl） | M2 不接 IM 入口：`dev-workflow` 的入参是 issue 形状对象，由验证脚本构造后 `createRun().start()`。飞书 inbound 属 M5，本里程碑不依赖 |
| 审批方 | **运行时无闸门** | M2 的 commit 直接落在本地 feature 分支，不 suspend。人工闸门在 M3 的 merge 步才引入 |
| 验收方 | **用户（人工）** | 用户规则：AC 全绿 + 人工验收通过，才算完成。验收动作 = 跑 `scripts/verify-local-write.js` + 亲自 `git log` 检查沙箱仓库 |
| 卡 Owner | 当前开发会话 | 推进并更新本卡 |

> ⚠️ **运行时无闸门 ≠ 无需人工把关**。M2 的把关在**里程碑层**（跑完人来看结果），不在**编排层**（不 suspend）。这是因为 M2 对远端零影响，放行的代价可控；M3 一旦要 push/merge，闸门必须回到编排层。

---

## 1. 目标（一句话）🟦

**给定一条 issue 形状的入参，dev-workflow 在隔离的沙箱仓库里真建 feature 分支 → 真起编码 agent 改文件 → 过三个质量闸门 → 真 commit 落本地分支；全程 `main` 不动、远端不动、围栏红线拦得住。**

---

## 2. 为什么现在做（排序理由）🟦

| 理由 | 说明 |
|---|---|
| **M1 闸门已关** | M1 AC-1~AC-7 全绿且人工验收通过（2026-09-04）。按用户规则，M2 是当前唯一可推进的里程碑 |
| **这是第一次「真写」** | M1 全程只读。M2 是第一次让 agent 真改文件、真 commit——**自动开发这一 product 命题的第一次实证** |
| **风险已在前置阶段排掉两个** | 编码后端连通性（M2-2，2026-09-07 用户终端跑通 tool_use 闭环）、目标仓库可配置（M2-1，`CODING_REPO_ROOT`）均已完成 |
| **零远端 = 试错成本最低** | 不 push、不开 PR，最坏结果只是沙箱仓库变脏（`git reset --hard` 即可回滚）。红线敢在这里真刀真枪地验 |
| **暴露围栏是否真拦得住** | `guard.ts` 的 PreToolUse 拦截从没实测过。它是「自动开发敢不敢真写」的安全底线，越早证伪越好 |

---

## 3. 范围 / 明确不做 🟦

**做**

- 目标仓库可配置（`CODING_REPO_ROOT` 指向隔离沙箱），checkout/coding/commit 落在同一仓库
- `checkout` 真建分支 `feat/<issueNumber>-<slug>`
- `coding` 真起 Claude Code CLI 改文件（`git diff` 可见）
- `test` / `review` / `commit` 三闸门跑通，输出能被 zod 结构化解析（**仍是 LLM 自评**）
- `commit` 真落盘到本地 feature 分支
- **红线实证**：受保护路径、危险命令、越界写入三类拦截真拦得住
- 端到端验证脚本 + AC 矩阵 + flow 图

**明确不做**（留给后续里程碑）

- **不 push、不开 PR**（M3）；**不 merge**（M3，且是人工闸门）
- **不真跑 `npm test` / 真喂 `git diff` 给 review / commitlint 真校验**（M4）——M2 三闸门仍是 LLM 自评，只要求输出结构化
- **不接飞书 inbound、不做按钮回调**（M5）
- 不做多仓库、不做 GitHub App 鉴权、不做幂等防重复（M5）
- 不做 run 追踪 / 成本回写 / 失败重试告警（M6）

---

## 4. 端到端演示效果 🟦

跑完 `node scripts/verify-local-write.js` 后，在 **`D:\code\pr-agent-sandbox`** 里能看到：

```
$ git log --oneline -3
a1b2c3d (HEAD -> feat/1-add-install-section) docs: add install section to README
dab8cc1 (main) chore: 初始化 M2 沙箱靶场仓库

$ git branch --show-current
feat/1-add-install-section
```

同时**终端输出**逐条打印 AC 判定（分支名 / 改动文件数 / codingResult 是否占位 / 三闸门结构化字段 / 红线拦截日志 / main 与远端是否未动）。

**对标物**：沙箱仓库多出一个带真实 commit 的 feature 分支，且 `main` 与 `pr-agent` 工作树都干净。

---

## 5. 数据流向图 🟨

见 [`M2-本地写入闭环-flow.md`](./M2-本地写入闭环-flow.md)（Mermaid，含 guard 拦截点与零远端边界）。

ASCII 速览（M2 范围内）：

```
[验证脚本] 构造 issue 入参
      │
      ▼
 dev-workflow ── checkout ──▶ 沙箱仓库 建 feat/<n>-<slug>
      │
      ├── coding ──▶ ClaudeSDKAgent ──▶ Claude Code CLI
      │                    │
      │                    └── PreToolUse hook ──▶ guard.ts ── ✗ deny（红线）
      │                                              │
      │                                              └── ✓ allow ──▶ 读写沙箱文件
      ├── test / review / commit（三闸门，LLM 自评 + zod 结构化）
      │
      └── [stopAfterCommit] ✂ 截断：push-open-pr / notify / merge 不执行
                                    （远端零接触）
```

---

## 6. 用到的 Mastra 模块 🟨

| Mastra 模块 | 导入路径 | 职责 | 为什么用它，而不是裸写 |
|---|---|---|---|
| `Workflow` / `createStep` | `@mastra/core/workflows` | 强制步骤顺序，保证「没编码就没测试、没审核就没 commit」 | 顺序即闸门。裸写脚本可以随意跳过，`Workflow` 的 `.then()` 链让跳过变成显式改造 |
| `ClaudeSDKAgent` | `@mastra/claude` | 编码执行体，真读写文件 | 复用 Claude Code CLI 原生工具链（Read/Edit/Write/Bash/Glob/Grep）。自造 `createTool` 要重实现一整套 + 错误恢复，每个边界都是漏洞点 |
| PreToolUse hook | `@anthropic-ai/claude-agent-sdk` | 唯一可靠拦截点 | `bypassPermissions` 下 `canUseTool` 回调**根本不会被调用**（SDK 源码明确）。只有 hook 能拦住每一次调用 |
| `agent.generate` + `structuredOutput` | `@mastra/core` | 三闸门输出结构化，供条件边判定 | 避免解析字符串。M4 换真判据时，字段名不用动 |

**选型备注**：评估过「Mastra 原生 agent + 自造工具链」（可绕开 CLI 依赖与 `reg.exe`），因工具链与错误恢复成本过高放弃，理由同上表第 2 行。

---

## 7. 任务分解 🟨

### M2-1 目标仓库可配置 ✅ 已完成（2026-09-07）
- 内容：`adapters/github.ts` 的 `repoRoot()` 与 `agents/coding-agent.ts` 的 `getRepoRoot()` 统一读 `CODING_REPO_ROOT`；已设但目录不存在 / 缺 `.git` 时**显式抛错**而非静默回退 cwd。建沙箱仓库 `D:\code\pr-agent-sandbox`（本地 `main`，`dab8cc1`）
- 验收：4 个用例全过（未配置回退 pr-agent / 指向沙箱 / 不存在抛错 / 非 git 抛错）

### M2-2 编码后端连通性探针 ✅ 已完成（2026-09-07）
- 内容：`scripts/_coding-probe.js` 在 `%TEMP%` 一次性 git 仓库里驱动 `getCodingAgent()`，完成 Read→Write 的 tool_use 闭环
- 验收：用户在自己终端跑通，日志 `logs/coding-probe.log` 显示 `hello.upper.txt 是否存在 = true`，耗时 195s

### M2-3 编排改造：让 workflow 能停在 commit
- 内容：`ContextSchema` 加可选字段（如 `stopAfterCommit?: boolean`，默认 `false`）。为真时 `push-open-pr` / `notify` / `merge` 三步直接 `return inputData`。默认 `false` 保证 M3 行为不变
- 验收：`stopAfterCommit=true` 跑完不 push、不 suspend；`false` 时行为与改造前一致
- ⚠️ **为什么必须做**：`devWorkflow` 现在是 `.then()` 八步全串（含 push / merge）。不截断，M2 会真 push 或卡在 merge 的 suspend 上，两者都违反「零远端」

### M2-4 端到端验证脚本 `scripts/verify-local-write.js`
- 内容：设置 `CODING_REPO_ROOT` → 构造最小 issue 入参 → `createRun().start()` → 逐条打印 AC 判定（分支名 / 改动文件数 / codingResult 是否占位 / 三闸门字段 / 红线日志 / main 与 pr-agent 是否未动）
- 验收：脚本可重复运行，输出能直接对应 AC 表

### M2-5 红线实证（**优先做**）
- 5a **纯函数层**：单测 `guardToolCall()` —— 写 `agent.md` / 写 `.env.local` / `git push -f` / `git reset --hard` / `rm -rf` / 写仓库外路径，逐条断言 `decision === 'deny'`
- 5b **端到端层**：让编码 agent 在沙箱仓库里被要求改 `agent.md`（沙箱内预置一个），确认被 hook 拦下且文件未被改
- 5c **失控上限**：`CODING_MAX_TURNS=1` 跑一次，确认编码在没完成任务时**显式失败**而非静默截断
- 验收：5a 全绿；5b 有 `[permission-guard] deny` 日志且 `agent.md` 未变；5c 结果可判别

### M2-6 端到端跑通沙箱闭环
- 内容：在沙箱仓库真跑 `checkout → coding → test → review → commit`
- 验收：AC-1~AC-10 全绿，证据落 `logs/m2-verify.log`

### M2-7 AC 矩阵回填 + 人工验收
- 内容：AC 表「验证方式·证据位置」列回填实际脚本路径；状态改「待人工验收」；用户 `git log` 亲自确认后标记完成

---

## 8. 验收清单（AC）🟨

| # | 验收项 | 判据 | 验证方式 · 证据位置 |
|---|---|---|---|
| AC-1 | checkout 真建分支 | 沙箱 `git branch --show-current` = `feat/<n>-<slug>`，非 `main` | `verify-local-write.js` 打印；证据 `logs/m2-verify.log` |
| AC-2 | coding 真改文件 | 沙箱 `git status --short` ≥1 个改动文件，且路径在沙箱内 | 同上 |
| AC-3 | codingResult 非占位 | `codingResult` 不以 `(SKIPPED_NO_CREDENTIALS` / `(ERROR:` 开头 | 脚本断言；证据 `logs/m2-verify.log` |
| AC-4 | 红线：受保护路径 | 写 `agent.md` → `deny`，且文件 mtime/内容未变 | 5a 单测 + 5b 端到端；日志含 `[permission-guard] deny` |
| AC-5 | 红线：危险命令 | `git push -f` / `git reset --hard` / `rm -rf` → `deny` | 5a 单测；证据测试输出 |
| AC-6 | 红线：越界写入 | 写沙箱仓库根之外的路径 → `deny` | 5a 单测 |
| AC-7 | 三闸门结构化输出 | `testResult.passed` / `reviewResult.decision` / `commitResult.message` 均存在且类型正确 | 脚本打印 + zod 解析成功 |
| AC-8 | commit 真落盘 | 沙箱 feature 分支 `git log -1` 是新 commit，message 为 Conventional Commits | 脚本打印 `git log --oneline -3` |
| AC-9 | 零远端 | `git log origin/main..HEAD` 无输出；无新远端分支；`main` 的 commit 数与运行前一致 | 脚本打印 main sha 前后对比 |
| AC-10 | 目标仓库隔离 | `D:\code\pr-agent` 工作树 `git status --short` 干净，无新增 feature 分支 | 脚本打印 pr-agent 的 `git status` + `git branch` |

> **AC-4/5/6 是本里程碑的核心价值**：它们验证的不是「功能能不能用」，而是「失控时拦不拦得住」。这三条没过，M3 就不该开。

---

## 9. 解锁与复用 🟨

- **本里程碑预期解锁**（供 M3）：
  - `CODING_REPO_ROOT` 目标仓库配置口子 → M3 换远端仓库只需改 env
  - 已实证的 `guard.ts` 围栏 → M3 敢真 push（红线已被证明有效）
  - `stopAfterCommit` 编排开关 → M3 默认 `false` 即恢复完整八步
  - 端到端验证脚本骨架 → M3 扩展成 PR 闭环验证
- **实际复用**：<M3 建卡后回填>

---

## 10. 异常与兜底 🟨

> 判定条件必须可执行（返回码 / 超时秒数 / 配置缺失），不许写「可能出问题就兜底」。

| 已知异常 | 判定条件（可执行） | 降级行为 | 是否阻断 AC |
|---|---|---|---|
| 编码后端凭据缺失 | `missingCodingCredentials()` 返回 `true` | coding 步返回 `(SKIPPED_NO_CREDENTIALS: 编码未执行)` 并 `console.error` | **是**（AC-2/3） |
| 编码 agent 执行抛错 | `agent.generate()` reject | 返回 `(ERROR: <msg>)` 并 `console.error` | **是**（AC-2/3） |
| **CLI 子进程挂起** | `generate()` 超过 N 秒未返回（当前**无超时守卫**） | 无 → 整条 workflow 冻住 | **是**（全部）。**M2 待补**：加 `CODING_TIMEOUT_MS` 守卫 |
| `CODING_REPO_ROOT` 非法 | 目录不存在 / 缺 `.git` | `getRepoRoot()` 显式抛错 | **是**（全部） |
| checkout 建分支失败 | `githubCheckout()` reject | **当前 catch 后返回占位分支名 `feat/<n>-dev` 并仅 warn** | ⚠️ **隐患**：AC-1 会误判通过。M2 待改：失败应显式暴露并阻断 |
| commit 无改动可提 | `gitCommit()` 返回 `committed:false, error:'nothing-to-commit'` | 仅 warn，流程继续 | **是**（AC-8） |
| 闸门结构化解析失败 | `structuredOutput` 的 `errorStrategy:'strict'` 抛错 | workflow 失败 | **是**（AC-7） |
| 沙箱仓库工作区不干净 | 运行前 `git status --short` 非空 | 无 → 无关改动可能被 `git add -A` 卷进 commit | **是**（AC-8）。M2 待加：运行前前置检查 |
| 沙箱被污染需重来 | 人工判断 | `cd D:\code\pr-agent-sandbox && git checkout main && git reset --hard <初始sha> && git branch -D feat/*` | — |

> **三条「M2 待补/待改」是本卡从设计期就暴露的缺口**，不是事后发现：coding 步无超时守卫、checkout 失败静默降级、缺运行前洁净检查。它们的共同点是**失败不显式**，会让人看到「全绿」的假象——M1 的 LLM 降级就是这么踩的。

---

## 11. 实施日志 🟨

### 2026-09-07 · M2-1 目标仓库可配置 + 沙箱仓库建立
- **改了什么**：
  - `src/mastra/adapters/github.ts` — `repoRoot()` 优先读 `CODING_REPO_ROOT`，非法时抛错
  - `src/mastra/agents/coding-agent.ts` — `getRepoRoot()` 同上，两处读同一 env
  - `src/mastra/README.md` — §3.3 补「目标仓库由 `CODING_REPO_ROOT` 决定」
  - `scripts/_coding-probe.js` — 修两处误导性旧文案
  - 新建 `D:\code\pr-agent-sandbox`（本地 `main`，`dab8cc1`）
- **重点模块**：`src/mastra/adapters/github.ts:52`（`repoRoot`）、`src/mastra/agents/coding-agent.ts:43`（`getRepoRoot`）
- **本轮核实的既有事实**：
  - `githubCheckout()`（`adapters/github.ts:321`）是**纯本地 git 操作** —— `createBranchVerified` + `git checkout`，不调 GitHub API、不 push。**因此沙箱仓库无需建在 GitHub 上**，M3 要 push 时再补远端
  - `dev-workflow.ts:314-328` 是 `.then()` 八步全串，**含 push-open-pr 与 merge** → M2 必须做截断改造（M2-3）
  - `ContextSchema`（`dev-workflow.ts:47-58`）入参为 issue 形状：`issueNumber` / `issueTitle` / `issueBody` 三项**必填**，与 M1 的 `{ query }` 不同
  - `guard.ts:47-53` 受保护路径：relative to repoRoot → 沙箱内预置 `agent.md` 即可测红线
  - `coding-agent.ts:242` `maxTurns` 默认 30，env `CODING_MAX_TURNS` 可覆盖
- **踩坑**：（无）4 个正反向用例一次通过
- **待确认**：
  - **任务输入口径**：用户未就「沿用 query / 指定 issue / 固定 demo」拍板。**按代码现状，ContextSchema 已是 issue 形状**，故 M2 首版采用「脚本构造最小 issue 对象」驱动，M3 再接真实 issue 来源。若用户要求改成 `{ query }`，需同步改 `ContextSchema`
  - **M2-3 截断方案**：推荐 `stopAfterCommit` 可选字段（默认 `false`），待用户确认后再动编排

### 2026-09-07 · M2-2 编码后端连通性探针（用户在自己终端跑通）
- **改了什么**：无代码改动；用户在本地终端执行 `node scripts/_coding-probe.js`
- **重点模块**：`scripts/_coding-probe.js`（`%TEMP%` 一次性 git 仓库靶场）
- **本轮核实的既有事实**：
  - 日志 `logs/coding-probe.log`：耗时 195s，`hello.upper.txt 是否存在 = true`，内容大写正确 → **tool_use 多轮闭环可用**
  - `reg.exe` 拦截是 **WorkBuddy 沙箱 Program Blacklist 特有**，用户普通终端不存在该黑名单
  - 后端走 `~/.claude/settings.json` 的 cc-switch 代理 `127.0.0.1:15721`（`ANTHROPIC_BASE_URL`），**不是** `LLM_*` 中转
- **踩坑**：探针日志里「内部已把 LLM_* 映射」是写死的旧文案（映射已删），已修正
- **待确认**：M2-6 端到端若仍超时，需确认是否又是 reg.exe —— 若是，端到端验证只能在用户终端跑
