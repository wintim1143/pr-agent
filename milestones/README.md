# pr-agent 里程碑规划

> ⚠️ **本文档取代旧 S0–S6 纵向切层规划。** 根目录 `11-IM驱动的多Agent自动开发工作流设计.md` / `12-自动开发Agent任务列表与验收标准.md` / `13-卡点与待确认清单（编码前）.md` 已归档，仅作历史参考；新规划以本目录为准。
>
> 本目录是**唯一的规划真相源**。大的路线图看本文件，单个里程碑的细节与「改了什么」看对应里程碑卡。
>
> 规则：每完成一个**小任务**，就在对应里程碑卡的「实施日志」章节追加一条记录
> —— 改了哪些文件、哪个是重点模块、踩了什么坑。目的是让这些文件本身成为可回溯的学习材料。

---

## 为什么重排：从「纵向切层」改成「横向切片」

旧的 S0–S6 是按**技术层**纵向切的（S2 编码、S3 闸门、S4 端到端、S5 飞书……），
问题是**只有走到 S4 才能第一次看到完整链路跑起来**，前几步都是看不见的地基。

新的划分原则：**每个里程碑都是一条端到端可演示的切片**，功能从薄到厚，每一步都有可见产出。

| | 旧（S0–S6，纵向切层） | 新（M1–M6，横向切片） |
|---|---|---|
| 组织方式 | 先做地基，最后才串联 | 先跑一条最细的线，再逐段加粗 |
| 首次可见效果 | S4（第 4 步） | **M1（第 1 步）** |
| 风险暴露 | 晚期集中爆发 | 每个里程碑暴露一批 |
| 对目标仓库的写入 | S2 就要写 | **M1、M2 完全不写远端** |

---

## 里程碑总览

> **规划基线已完成（原 M0 的实物产出：本 README + M1 卡 + `_template-里程碑卡.md`），不单列里程碑，后续从 M1 起算。**

| 里程碑 | 名称 | 端到端演示的效果 | 是否写目标仓库 | 状态 |
|---|---|---|---|---|
| **M1** | 只读洞察闭环 | 飞书发一句话 → 拉 GitHub issue/commits → LLM 汇总 → 卡片回推 → 人工点按钮 resume | **否**（只读 REST） | ✅ **已完成**（AC-1~AC-7 全量复验通过 + 人工验收通过） |
| **M2** | 本地写入闭环 | 真起编码 agent 在 feature 分支改文件 + 真 commit，**不 push** | 仅本地分支 | ✅ **已完成**（AC 全绿 + 人工验收通过，2026-09-15 归档）—— 卡：[`M2-本地写入闭环.md`](./M2-本地写入闭环.md) · 验收操作：[`M2-人工验收手册.md`](./M2-人工验收手册.md) |
| **M3** | 完整 PR 闭环 | push + 开 PR + 人工确认 → squash merge | 是 | ✅ **已完成（2026-09-15）**——八项任务全过：M3-1 靶场+写权限 / M3-2 目标仓库切换 / M3-3 真 push+真开 PR / M3-4 飞书卡片（含 PR 链接）/ **M3-5 跨进程 suspend-resume（含真 squash merge）** / M3-6 远端红线 / M3-7 验证脚本+AC 回填 / M3-8 判负即终止。⚠️ 遗留：**AC-7 需人工去飞书群确认**、远端 PR #1/#2 与 `feat/*` 分支待清理 —— 卡：[`M3-完整PR闭环.md`](./M3-完整PR闭环.md) |
| **M4** | 真质量闸门 | 把**程序可判定的事实**从 LLM 手里收回：测试真跑取 exit code；`passed` 拆为 `testsPassed`（程序）+ `requirementMet`（LLM）后合成 | 是 | ✅ **已完成**（2026-09-15）· 单测 174/174 · `--adapter` 17/17 · `--path-a` 10/10 · `--path-b` 11/11 · `--path-c` 9/9 —— 卡：[`M4-真质量闸门.md`](./M4-真质量闸门.md) · 图：[`M4-真质量闸门-flow.md`](./M4-真质量闸门-flow.md) |
| **M5** | 多仓库与幂等 | 同一实例服务多个仓库（`target` 显式指定，全链路 per-target）；入口去重使重复触发不产生重复 run / PR | 是 | ✅ **已完成**（2026-09-16）· 单测 243/243 · `--contract` 8/8 · `--dedup` 6/6 · `--serial` 3/3 · `--multi` 13/13 —— 卡：[`M5-多仓库与幂等.md`](./M5-多仓库与幂等.md) · 图：[`M5-多仓库与幂等-flow.md`](./M5-多仓库与幂等-flow.md) |
| **M6** | 可观测与成本 | 把「已打但关联不起来」的日志升级为**可关联的运行追踪**：统一日志规范 + 每事件带真实 `runId`/`traceId` + 按 run 重建八步时间线 + token 成本按仓库归口 | — | ✅ **已完成（2026-09-17）**（卡 [`M6-可观测与成本.md`](./M6-可观测与成本.md) · 图 [`M6-可观测与成本-flow.md`](./M6-可观测与成本-flow.md)；单测 **291/291** · `--all` **34/34**；§0.1 三条待拍板 **3/3 已拍板**。⚠️ 收口时还修掉一个 **M5 遗留的 SQLite 写竞争缺陷**（`@libsql/client` 默认 `busy_timeout=0`），见卡 §11 踩坑 7） |
| **M7** | 飞书双向控制 | 群里发一句话 → 起 dev-workflow → 卡片回推（含仓库/分支/runId/PR 链接） → **群里点按钮完成合并/拒绝（真 resume）** | 是 | 待立项确认（卡 2026-09-16 建于 [`M7-飞书双向控制.md`](./M7-飞书双向控制.md) · 图 [`M7-飞书双向控制-flow.md`](./M7-飞书双向控制-flow.md)；**§0.1 五条待拍板 · 1/5 已定**：回调标识 = 结构化 value） |

> ⚠️ **M4 范围变化（2026-09-15 据实修订）**：原定义的三项 ——
> ① `git diff` 真喂 review：**已在 M2 收尾时完成**（且扩到 test/commit 三闸门，见 M2 卡 §11 归档日志）；
> ② commitlint 真校验：**经核实不适用** —— 本项目无 commitlint（无依赖 / 无配置 / 无 husky），原来让 LLM 回答的 `lintPassed` 字段已删除；
> ③ `npm test` 真跑：**唯一剩余项**。
> 因此 M4 的重定义收敛为一句话：**把凡是程序能判定的字段（测试结果、lint 结果、diff 存在性）从 LLM 手里收回，改为程序写入**。
>
> 📌 **建卡后补充的两条约束（2026-09-15，详见卡 §1 / §3）**：
> - **第 0 号前置：靶场无测试可跑**。`wintim1143/pr-agent-e2e` 是 README-only 仓库、**无 `package.json`**
>   → 「真跑 `npm test`」**当前无对象**。M4 必须先给靶场补「被测文件 + **人工预置**测试」。
>   ⚠️ 测试**必须人写** —— 若由 coding agent 自己写，就是「自己出卷自己判卷」，断言可以恒真，零信息量。
> - **必须拆字段，不能让程序覆写 `passed`**。`passed` 底下压着两个性质完全不同的问题：
>   ①「测试过了吗」（**事实**，需执行能力 → 归程序）②「需求实现了吗」（**语义**，需阅读理解 → 归 LLM）。
>   合成一个布尔值之后，事后无法分辨「判负是因为测试红」还是「模型认为需求没做」—— 排障时这条信息最贵。
>   完整判据归属表见卡 §1。⚠️ `testsPassed = null`（无测试可跑）**绝不可等价于 `true`** ——
>   这与 M2 那次让 LLM 回答 `lintPassed` 的幻觉是同构错误。

> ⚠️ **M5 范围变化（2026-09-16 据实修订）**：原定义的三项，收敛为两项 ——
> ① **GitHub App 鉴权 → 剔除**，拆为独立后置项。依据：**多仓库并不要求 GitHub App** ——
>    fine-grained PAT 的 Repository access 本就支持多选，且现已全程实测跑通 push / 开 PR / merge。
>    App 的收益（installation token 自动轮换、审计不挂个人、webhook 推送前提）属**长期运维 / 多租户**议题；
>    塞进 M5 会让一个里程碑变成两个。
> ② **幂等防重复触发 → 保留，但范围缩小**。侦察发现「防重」有两层且**上层已做、下层才是缺口**：
>    - **产物层（已做，M3）**：`githubPushAndOpenPR` 开 PR 前先查同 head 的 open PR 并复用（避免 422 重复建），
>      `githubCheckout` 也复用既有分支。→ **不改实现，只补回归断言**
>    - **入口层（缺口，M5a）**：`api.controller.ts` 自认「真实去重应在持久化游标上做，此处为最小实现」。
>      配合 `sinceTs` 默认取「一小时前」，一旦挂上定时轮询，同一条消息会被重复触发几十次
>      → 重复的 run 会重复跑 coding / review（真金白银的 LLM 调用），并让多个 run 争抢同一本地 clone
> ③ **RepoTarget 多仓库 → M5b，本里程碑主体**。
>
> 📌 **M5 最重要的一条设计约束**：`target` **只把逻辑标识（`owner`/`repo`/`baseBranch`）写进 `ContextSchema`，
> 本机 clone 路径绝不写入** —— 因为 `ContextSchema` 会被序列化进 snapshot，而 M3-5 的跨进程 resume 正是靠它恢复上下文；
> 写入本机绝对路径会让 run 快照**绑定到某台机器的文件系统**。本机路径由「仓库注册表」在运行时解析。
>
> 📌 **M5 的风险性质与 M4 不同**：M4 的风险是**假通过**（报告绿了但没验），
> M5 的风险是**静默损坏**（重复消耗 / 两个 run 争抢同一 clone 导致提交落到错误分支）。
> 后者不崩溃、不报错，因此判据必须是可数的事实（有没有第二个 run / 有没有串台），不能是「看起来正常」。
> 完整范围与 AC 见卡 §3 / §8。

> 📌 **M6 建卡说明（2026-09-16）**：M6 的定义**据实收敛**了 —— 不再写成「run 追踪 / 成本回写 / 失败告警」三条并列，
> 而是先回答一个问题：**现状到底缺什么？** 实测答案是「**缺关联，不缺日志**」：
> - `logs/dev-workflow.log` 已有 **701 行、0 非 JSON、12 种事件** —— 事件不缺；
> - 但 `runId` 字段虽在 `progress.ts:85` 已声明，**真实覆盖率 = 0/701**
>   （唯 6 条带 `runId` 的记录，值是字面量 `"rejected"` / `"resumed"` —— 见下条）；
> - 因此**多 run 事件在同一文件里交错，无法按 run 分组**，重建时间线只能绕道
>   `mastra.db` 反查 runId 再手工切分（这条手法记在 `.workbuddy/memory/ref-ops.md`）。
>
> 🔑 **根因是「位置参数误用，类型系统拦不住」**：`stageStart(stageName: string, runId?: string)` 两个形参都是
> `string`，于是 `stageStart('merge', 'rejected')` 把语义串塞进了 `runId` 槽而**编译器不报错**。
> 同时 `merge` 步的 execute 压根没解构 `runId`（而 Mastra **本来就免费提供** —— 其余六步都已解构）。
> → **M6 的修法不是加注释提醒，而是把位置参数改成对象参数，让误用无法通过编译。**
>
> 📌 **用户明确追加的要求（2026-09-16，已写入卡 §3）**：M6 **必须包含统一日志规范**；
> **有意义的输出不允许只打 `console`，至少落文件**；**最好有类似 OTEL 的 `trace_id` / `run_id`**。
> ⚠️ 但**不引入 OTEL 本体**：本项目当前没有观测后端，字段按 span 语义命名即为将来迁移留口
> —— 判据是「**可关联**」，不是「**可上报**」。
>
> 📌 **M6 → M7 是硬依赖，不是排期偏好**：飞书按钮回调的本质是「**用一个标识定位挂起的 run 并 resume**」，
> 而现状卡片用的标识是 `merge_<issueNumber>` —— 在**多仓库 + 重跑**下不唯一（见 M7 说明块）。
> **M6 之前这个标识不可信，M7 的按钮就是猜。**
>
> 📌 **「为什么不是替代 M6」也在此一并回答**：顺序由**谁阻塞谁**决定，不由「谁更接近目标」决定 ——
> 先做飞书会得到一个**能跑但无法验收**的里程碑（M7 的核心判据 AC-4「回调定位到正确的 run」只能靠
> 构造双 run + **查日志确认**来验，而 `runId` 覆盖率现在是 **0/701**）。
> **六条论证见 `M7-飞书双向控制.md` §2「排序论证」。**
> 📌 **「立项」与「开工」是两件事**：M7 卡 2026-09-16 就建了，但建卡是为了**把 6 处悬空承诺定性、把范围锁死**
> （执行下方全局原则第 9 条），**不构成插队理由**。**建卡防漂移，顺序按依赖。**

> 📌 **M7 立项说明（2026-09-16）—— 一次「无主债务」的收口，也是项目真正目标的落地**
>
> **① 用户表态（本里程碑的真实动机）**：「我希望的是能通过飞书直接控制这个项目。」
> 即不只是「让按钮能点」，而是**从群里一句话发起开发、并在群里完成人工闸门**。
>
> **② 它承接的债 —— 前四张卡写了 6 处「推迟到 M5」，但 M5 从未承接**：
>
> | 承诺点 | 原文 |
> |---|---|
> | `M2-本地写入闭环.md:19` / `:62` | 「飞书 inbound 属 M5」/「不接飞书 inbound、不做按钮回调（M5）」 |
> | `M3-完整PR闭环.md:30` / `:72` | 「M3 仍不接飞书 inbound —— 那是 M5」/「不接飞书 inbound / 卡片按钮回调（M5）」 |
> | `M4-真质量闸门.md:27` | 「M4 仍不接飞书 inbound —— 那是 M5」 |
> | `验证手册.md:93` | 「按钮回调（inbound）设计上推迟到 M5」 |
>
> **③ 为什么变成孤儿（技术原因，值得记住）**：M5 做了**另一条 inbound 通路** ——
> 群消息**轮询**（`adapters/inbound-poll.ts` + 游标 + 原子认领），解决「重复触发」。
> 而按钮回调是**事件推送**（长连接 / 回调服务器），解决「人工闸门回传」。**两者形态与作用对象都不同**：
> 轮询是**创建** run，回调是**唤醒** run。看起来同类，实际无关 → **承诺漂移得以静默发生。**
> 至今 `adapters/feishu.ts:17` 仍写「按钮回调 … 属 P1 inbound 工作」。
>
> **④ 立项前侦察出的七处缺口（决定了真实工作量，详见卡 §3）**：
> 最要紧的一条是 —— **飞书入口至今只起 `insight-workflow`（只读洞察），从来不起 `dev-workflow`（真开发）**
> （`api.controller.ts:89-94` 硬编码）；且 `dev-workflow` 要 `issueNumber`/`issueTitle`/`issueBody` 形状的入参，
> **飞书一句话喂不进去**。此外卡片按钮 value 是 `merge_<issueNumber>`（多仓库下不唯一）、
> 卡片不含 target、无长连接依赖（`package.json` 里没有飞书 SDK，现有能力是轮询）。
>
> ✅ **有利条件**：`.env` 已配 `FEISHU_APP_ID` + `FEISHU_APP_SECRET` + `FEISHU_RECEIVE_ID`（**自建应用模式**），
> 而 `card.action.trigger` 回调**只有自建应用支持** —— 飞书侧责任边界已具备，缺的只是本机接收端。
> ⚠️ 但本机在家庭网络**无公网入口** → 事件推送必须走**长连接**（§0.1 待拍板项 1），
> 或退到内网穿透 + 回调 HTTP 服务器。
>
> ⚠️ **本里程碑必须显式声明的两条风险**：① 群聊多人触发 → 并发压力上升，
> M5 已知的 `TTL(30min) > WAIT(10min)` 窗口会**被真实撞上**；
> ② **触发即执行 agent 写下的代码** → 「谁有资格触发」不是可选项，**准入控制不能被默认成「群里任何人」**。
>
> **⑤ 2026-09-17 拍板：回调标识改为结构化 `value`，不用任何分隔符**
> 用户先提议把分隔符从 `_` 换成 `:`（理由：冒号在**分词查询**上优于下划线）—— 判断成立，
> 但核查后取了更优解：`merge_<n>` 把「**哪个 run**（身份，用于定位）」与「**哪个动作**（意图，用于分支）」
> 压进同一个 token，与 M6 已修的 `stageStart('merge','rejected')` **同构**；而 `value` 在飞书侧
> **本就是 JSON 对象**（官方原文：「仅支持 key-value 形式的 JSON 结构，且 key 为 String 类型」）
> → **拼接是自造的，不是接口要求**。结构化让「分词 / 字符集 / 唯一性」三个问题同时消失。
> 形态：dev `{ kind:'dev', action:'merge'|'reject', runId, repoKey }` /
> insight `{ kind:'insight', action:'confirm'|'rerun', runId }`。
> 详见 `M7-飞书双向控制.md` §7 M7-4（含字段必要性表）与 §11 记录 2026-09-17。
> ⚠️ 同批更正一处**假约束**：`docs/archive/11-IM驱动的多Agent自动开发工作流设计.md:125` 的
> 「`callback_id` ≤128、仅字母数字下划线短横线」**在飞书官方文档里查无支撑**，勿引用。
> 完整范围 / 七项缺口 / 13 条 AC 见卡 §3 / §8。

### 与旧方案的映射（避免上下文丢失）

| 旧工单 | 落到哪个里程碑 |
|---|---|
| S0（拍板 D4 目标仓库） | **M3 建卡前已定**：新建专用靶场仓库（零污染；把「链路对不对」与「产物对不对」分开验）。实际仓库名 **`wintim1143/pr-agent-e2e`** |
| S1（HTTP 触发入口） | M1（只读版入口） → M3 补齐业务语义 |
| S2（真编码验收） | M2 + M3 |
| S3（真闸门，核心债） | M4 |
| S4（端到端闭环） | M1 是最细版本，M3 是完整版本 |
| S5（飞书入口，原在后置） | **提前到 M1**（只读版「推卡片」）→ **M5a 补入口幂等（收消息·轮询）** → **M7 补按钮回调（双向控制）** —— 这是 IM 驱动项目的主入口，不该后置 |
| S6（可观测 / 多仓库） | M5（多仓库）+ M6（可观测与成本） |
| （旧方案未列，M7 新增） | **飞书双向控制**：按钮回调被 M2/M3/M4 四处承诺「推迟到 M5」却未被 M5 承接（见上方 M7 立项说明），2026-09-16 用户明确「要能通过飞书直接控制这个项目」→ 立项 M7 |

---

## 里程碑间解锁关系

每个里程碑都是「薄切片」，但切片之间不是孤立的——前一个跑通后沉淀的可复用基建，直接变成后一个的起跑线。下表把这条递进链显式写出来（**M1–M7 卡均已建**；M6 卡与 M7 卡 2026-09-16 同日建。⚠️ M7 **建议排在 M6 之后实施** —— 见其行内依据）。

| 里程碑 | 沉淀的可复用基建 | 解锁的下一里程碑能力 | 前置输入/外部依赖 |
|---|---|---|---|
| **M1 只读洞察** | `githubIntegration` / `feishuIntegration`（继承 `Integration` 基类）、`insight-workflow` 四步编排骨架（collect→summarize→notify→confirm + `suspend/resume`）、LibSQLStore 跨请求恢复 | **M2**：复用同一 GitHub client 扩展出写分支 / commit；复用 workflow 骨架，把 step2 换编码 agent、step4 换「开 PR 确认」 | GitHub/飞书配置 + 中继可用 |
| **M2 本地写入** | `CODING_REPO_ROOT` 目标仓库配置口子、**已实证的 `guard.ts` 围栏**（受保护路径 / 危险命令 / 越界三类拦截）、`stopAfterCommit` 编排开关、端到端验证脚本骨架 | **M3**：换远端仓库只改 env；红线已实证故敢真 push；`stopAfterCommit=false` 即恢复完整八步 | 编码后端凭据（`~/.claude/settings.json` 的 cc-switch 代理）+ 沙箱仓库 `D:\code\pr-agent-sandbox` |
| **M3 完整 PR** | push + 开 PR + merge 全链路；**D4 目标仓库 = 专用靶场 `wintim1143/pr-agent-e2e`（已定）** | **M4**：在真实 PR 上挂真质量闸门 | 靶场仓库 + fine-grained token（Contents/Pull requests = write） |
| **M4 真闸门** | 程序侧**测试执行器**（`adapters/test-runner.ts`：探测 / 白名单执行 / 超时杀进程树 / 输出截断 / env 剥离凭据）、拆字段后的闸门契约（`testsPassed` 归程序 + `requirementMet` 归 LLM）、`agentModifiedTests` 自证检测（**默认阻断**）、`runGate` 重试回灌（P0-2） | **M5**：`runTests(root)` 只吃一个路径参数，换 `CODING_REPO_ROOT` 即可复用于多仓库（多仓库能力的第一块砖） | ✅ 靶场已补「`src/greet.js` + **人工预置**测试 `test/greet.test.js`」（零依赖 `node --test`，靶场 `main` = `21f4e7a`）；**无需新增任何外部凭据** |
| **M5 多仓库与幂等** | `RepoTarget`（**只含逻辑标识，不含本机路径**）+ 仓库注册表、per-target 的 `repoRoot()` / `getGithubConfig()` / **红线**、入口去重存储（唯一索引原子认领 + 游标持久化）、每仓库串行锁（防提交落错分支） | **M6**：在稳定的多仓库上做可观测与成本 —— `target` 是多维追踪的天然维度；去重表本身就是事件账本，可直接扩成 run 追踪入口 | 第二个靶场仓库（多仓库必须有第二个对象才可验证）；**无需新增任何外部凭据** |
| **M6 可观测与成本** | **统一日志规范**（`docs/日志规范.md` + 统一 logger 出口）、**可信 `runId`/`traceId` 贯穿全链路**（每事件带真实 run 值 + `<runId>:<stage>` span 语义）、按 run 重建时间线的读取器、token 成本按 `repoKey` 归口、`(runId, stage)` 的 start↔end 配对校验、`run:end` 终态 | **M7**：按钮回调的本质是「用标识定位挂起的 run 并 resume」—— M6 之后卡片才能携带**可信 runId**；点完按钮也能立刻拿 runId 查该 run 走到哪一步。**硬依赖，非排期偏好** | ✅ 全部就位：M5 的 `target` 是多维成本维度、M5 的去重表（含 `run_id` 列）是现成的事件→run 索引、M4 的 `test:run` 已带 `exitCode`/`durationMs`；**无需新增任何外部凭据** |
| **M7 飞书双向控制** | 常驻长连接接收端（消息 + 卡片回调）、约束式自然语言路由（显式仓库 + 白名单，**失败即拒答零 run**）、入口分流到 `dev-workflow`、按钮回调 → `resume`、卡片带 target/runId、准入控制 | （终点）项目形态完整：**从「六个里程碑拼出的自动化流水线」变成「在 IM 里能用的东西」** | ✅ 飞书侧已具备：`.env` 已配 `FEISHU_APP_ID`/`SECRET`/`RECEIVE_ID`（自建应用模式，**回调只有该模式支持**）；M5a 幂等 + M5b 串行锁即为「多人从群里触发」准备；M3 的 `suspend`/`resume` 与 `githubMergePR` **一行不用改**。⚠️ 待定：长连接实现（官方 SDK 需新增依赖）；⚠️ 本机无公网入口 |

---

## 全局原则

1. **每个里程碑必须有一条可演示的链路**，不接受「只加地基、看不到效果」。
2. **不写远端仓库的先做**。M1/M2 全部只读或只写本地分支，避免污染历史、也绕开了 D4（目标仓库选型）这个未决项。
3. **能用 Mastra 官方抽象就不裸写**。同样一个功能，优先落到 Mastra 的模块上（Integration / Tool / Workflow / Storage / Agent），
   而不是自己 new 一个 fetch 封装——这样踩过的坑和学到的东西是可迁移的。
4. **每张卡按 🟦定义期 / 🟨实施期 分层填写**（模板第 1–4 节定义期必填，第 5–10 节实施期渐进补全）。源码级证据（行号 / 实测命令 / 接口路径）只进「实施日志·已核实事实」，不进正文。
5. **完成后立刻归档**：改了什么、重点模块、踩坑，写进实施日志，不等到里程碑收尾。
6. **每个里程碑配一张数据流向图（流程图 / 时序图）**，展示「触发 → 编排 → 集成 → 外部系统」及 suspend/resume 断点。推荐用 Mermaid 写在独立文件 `M<n>-<名称>-flow.md`，卡片只放链接。
7. **记录不强制单文件**：里程碑卡可只做索引，链路图 / 流程图 / 实施日志 / 复盘等拆到同目录的独立文件（或子目录），便于单篇查看与演进。
8. **失败路径显式声明**：每个里程碑在卡的「异常与兜底」节列出「已知异常 → 判定条件 → 降级行为 → 是否阻断 AC」，判定条件必须可执行（返回码 / 超时秒数 / 配置缺失），不许写无法判定的表述。M1 的教训：外部依赖（LLM 中继）挂起曾冻结整条 suspend/resume 闭环，靠「超时+重试+降级」才解掉。
9. 🆕 **「不做项」不许悬空（2026-09-16 据 M7 立卡的教训补入）**：卡里写「本里程碑不做，**推迟到 M<n>**」——
   这是在开一笔**无主债务**：下个里程碑不主动承接，它就静默变成孤儿。
   **双向都要扫**：① 本卡开出的承诺，收口时逐条定性为「承接 / 移交某卡 / 声明不再做」；
   ② 前几张卡承诺给本卡的，建卡前 `grep -rn "推迟到 M\|留给后续" milestones/*.md` 捞出来核对。
   判定口诀：**「看起来有人负责」≠「有人负责」** —— 要能指出是哪张卡的哪个任务。
   反面教材：飞书卡片按钮回调被 M2/M3/M4 **四处**写成「推迟到 M5」，M5 卡却全文没有这一项，
   因为它们**形态不同**（M5 做的是轮询创建 run，回调是事件推送唤醒 run）→ 顺势漂了三个里程碑，最终由 M7 收口。

---

## 目录约定

```
milestones/
├── README.md                          ← 本文件：总路线图
├── _template-里程碑卡.md                ← 新里程碑照此模板建（🟦/🟨 分层）
├── 验证手册.md                          ← 三态验证矩阵（脚本直验 / HTTP / 人工）
├── M1-只读洞察闭环.md / -flow.md        ← ✅ 已完成
├── M2-本地写入闭环.md / -flow.md        ← ✅ 已完成（2026-09-15 归档）
├── M2-人工验收手册.md                   ← M2 的复现手册（已验收，保留作复现用）
├── M3-完整PR闭环.md / -flow.md          ← ✅ 已完成（2026-09-15）· 八项任务全过
├── M4-真质量闸门.md / -flow.md          ← ✅ 已完成（2026-09-15）· 七项任务全过
├── M5-多仓库与幂等.md / -flow.md        ← ✅ 已完成（2026-09-16）· 五个验证模式 30/30
├── M6-可观测与成本.md / -flow.md        ← ✅ 已完成（2026-09-17）· 单测 291/291 · `--all` 34/34
└── M7-飞书双向控制.md / -flow.md        ← 🟦 待开工（卡 2026-09-16 建；§0.1 五条待拍板 · 1/5 已定；**前置 M6 已完成，硬依赖已解除**）
```

单个里程碑卡统一包含十节（🟦=定义期必填，🟨=实施期渐进补全）：

1. 🟦 目标（一句话）
2. 🟦 为什么现在做（排序理由）
3. 🟦 范围 / 明确不做
4. 🟦 端到端演示效果
5. 🟨 链路图
6. 🟨 用到的 Mastra 模块（重点）
7. 🟨 任务分解（每个任务带验收）
8. 🟨 验收清单（AC）
9. 🟨 解锁与复用
10. 🟨 实施日志（**持续追加**）

> 数据流向图为必配项，但**不占这十节编号**——它独立成文件 `M<n>-<名称>-flow.md`，卡片 §5 只放链接。如需把链路图 / 实施日志也拆出去，同样独立成文件、卡片留索引即可。
