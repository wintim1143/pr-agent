# M<n> · <里程碑名称>

状态：<待开始 / 进行中 / 已完成>（<当前小任务>）
创建日期：<YYYY-MM-DD>

> **填写分层（先定义、后完善）**
> - 🟦 **定义期必填**：建卡当天即可写，不依赖任何代码——第 1–4 节。
> - 🟨 **实施期渐进补全**：推进到对应阶段再填，建卡时允许写 `TBD`——第 5–10 节。
> - ⚠️ **源码级证据只进第 10 节「实施日志·已核实事实」**（行号、实测命令、接口路径、版本号）。
>   第 5/6 节正文只写「选型结论 + 取舍理由」，不重复罗列一手证据。
> - 📊 **每个里程碑必配一张数据流向图**（流程图 / 时序图），展示「触发 → 编排 → 集成 → 外部系统」及 suspend/resume 断点。推荐用 Mermaid 写在独立文件 `M<n>-<名称>-flow.md`，卡片 §5 只放说明 + 链接。
> - 🗂️ **记录不强制单文件**：卡片可只做索引，链路图 / 流程图 / 实施日志 / 复盘等各自成篇（同目录 `M<n>-<名称>-*.md` 或子目录），便于单篇查看与演进。

---

## 1. 目标（一句话）🟦

**<一句话说清这条端到端链路从哪触发、经过什么、最后产出什么可见效果。>**

---

## 2. 为什么现在做（排序理由）🟦

| 理由 | 说明 |
|---|---|
| <理由 1> | <说明> |
| <依赖已解除 / 风险已暴露 / 效果可见> | <说明> |

---

## 3. 范围 / 明确不做 🟦

**做**
- <...>

**明确不做**（留给后续里程碑）
- <...>

---

## 4. 端到端演示效果 🟦

**<一句话描述完成时用户 / 群里能看到什么。验收「这条切片是否真跑通」的对标物。>**

---

## 5. 数据流向图（流程图 / 时序图）🟨（必配，建议独立成文件）

> **每个里程碑必配一张展示数据流向的图**（流程图或时序图），需要画出「触发 → 编排 → 集成 → 外部系统」的数据走向，以及 suspend/resume 这类跨请求断点。
> - **推荐**：用 Mermaid 写在独立文件 `M<n>-<名称>-flow.md`，便于渲染、复用、演进；卡片本节只放说明 + 链接。
> - 卡片内可保留一张 ASCII 速览图，详细图进独立文件。

**数据流向图文件**：[`M<n>-<名称>-flow.md`](./M<n>-<名称>-flow.md)（Mermaid 流程图 / 时序图，展示数据流向）

（可选 ASCII 速览：<ASCII 图：触发层 → 编排层 → 集成层 → 外部系统。标出 suspend/resume 跨请求断点。>）

---

## 6. 用到的 Mastra 模块（本里程碑重点）🟨（TBD）

> 本节**只写选型结论 + 取舍理由**（为什么用它而不是裸写）。
> 源码行号、实测命令、接口路径一律归第 10 节，不写在这里。

| Mastra 模块 | 导入路径 | 职责 | 为什么用它，而不是裸写 |
|---|---|---|---|
| <模块> | `<导入路径>` | <职责> | <取舍理由> |

**选型备注**：<有没有评估过但放弃的方案？依据一句话，细节归实施日志。>

---

## 7. 任务分解 🟨（TBD）

### M<n>-1 <任务名>
- 内容：<...>
- 验收：<可判定的标准>

### M<n>-2 <任务名>
- 内容：<...>
- 验收：<...>

---

## 8. 验收清单（AC）🟨（实施前期补全）

> 定义期可先列预期判据，实施到对应阶段再按实际端点 / 字段校准，不要过早钉死路径。

| # | 验收项 | 判据 |
|---|---|---|
| AC-1 | <验收项> | <可判定的判据，尽量带数字或具体输出> |

---

## 9. 解锁与复用 🟨（定义期先写预期，实施后回填）

- **本里程碑预期解锁**：<完成后为下一个里程碑沉淀哪些可复用基建 / 解锁哪部分能力>
- **实际复用**：<实施后回填：哪些模块 / 工具被 M<n+1> 直接复用>

---

## 10. 实施日志 🟨（持续追加）

> 每完成一个小任务追加一条，不要等到里程碑收尾才写。
> 格式：日期 · 任务 / 改了什么 / 重点模块 / 本轮核实的既有事实 / 踩坑 / 待确认。
> **本轮核实的既有事实**：仅此处记录源码行号、实测命令、接口路径等一手证据。

### <YYYY-MM-DD> · M<n>-<k> <任务名>
- **改了什么**：<新增/修改的文件，逐条列出>
- **重点模块**：<本任务最值得回看研究的文件与位置，写成 `path:line` 形式>
- **本轮核实的既有事实**：<动手前用命令/文档/源码验证过的前提，避免下次重新推导>
- **踩坑**：<错误信息 + 根因 + 解法>
- **待确认**：<需要用户拍板或需要外部条件的事项>
